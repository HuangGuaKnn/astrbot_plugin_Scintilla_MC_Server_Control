# 统一换行符：仓库内一律 LF，工作区 Windows 检出 CRLF
* text=auto eol=lf

# 二进制文件不要做换行转换
*.png binary
*.jpg binary
*.zip binary

# 发布包（git archive / Release 附件）里不带测试目录
/tests/ export-ignore

# 内部核验/交接文档：留在仓库备查，但不进发布包
/docs/VERIFY_*.md export-ignore
