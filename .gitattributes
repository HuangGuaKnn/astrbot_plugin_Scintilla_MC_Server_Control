# 统一换行符：仓库内一律 LF，工作区 Windows 检出 CRLF
* text=auto eol=lf

# 二进制文件不要做换行转换
*.png binary
*.jpg binary
*.zip binary

# 发布包（git archive / Release 附件）里不带测试目录
/tests/ export-ignore

# 内部核验/交接文档：留在仓库备查，但不进发布包
/docs/VERIFY_*.md export-ignore

# 开发自测脚本：v0.23.1 的插件市场审核报告里，审查员点名
# 「subprocess 仅出现在随包附带的开发自测脚本（run_v022*.py）中且只执行本地测试文件」——
# 判定虽属无害，但它们本来就不该进用户包：含本机解释器路径等开发者环境信息，
# 也给安全审查添了无谓的噪音面。留在仓库里照常可跑，只是不再随 Release 附件分发。
/run_*.py export-ignore
