{
  "connection": {
    "description": "① 连接与服务端",
    "hint": "RCON 连接参数与服务端目录。服务器事件转发、物品词典、知识库都依赖「服务端目录」。",
    "type": "object",
    "items": {
      "remote_rcon_mode": {
        "description": "异地 RCON 模式（AstrBot 与服务端不同机、只映射 RCON 端口）",
        "hint": "开启后只走 RCON，可以不填「服务端目录」；但依赖本地文件的整块能力会按设计禁用：物品词典、知识库、服务器事件播报、版本探测、进程指标。终端与 WebUI 都会明确告知。",
        "type": "bool",
        "default": false
      },
      "server_dir": {
        "description": "服务端目录",
        "hint": "服务端根目录（整合包 / 插件服务端解压后的那一层），例如 D:\\Minecraft\\Servers\\MyPack；读取 logs/latest.log 、mods/ 、plugins/ 均以它为根。保存时会做结构校验（logs/ 、libraries/ 、server.properties 、eula.txt 等特征），不像服务端根目录会拒绝保存。",
        "type": "string",
        "default": ""
      },
      "server_version_override": {
        "description": "手动声明服务端版本（留空 = 自动探测）",
        "hint": "例：1.20.1。**异地 RCON 模式下探测必然失败**，此时必须在此手填，否则所有附魔 / NBT / 物品组件请求会被拒绝（插件会明确告知原因）。优先级：手动声明 > 文件探测 > 未知。这里填的是你**明确知道的事实**，不是猜测。",
        "type": "string",
        "default": ""
      },
      "item_syntax_override": {
        "description": "物品语法世代（auto = 按版本自动判定）",
        "hint": "auto：按探测/声明的版本自动选（1.20.5 及以上用物品组件 []，以下用 NBT {}）。legacy_nbt：强制 NBT 写法。components：强制物品组件写法。**仅当自动判定与你的服务端不符时**才手动指定 —— 选错会生成解析失败的命令。",
        "type": "string",
        "default": "auto",
        "options": [
          "auto",
          "legacy_nbt",
          "components"
        ]
      },
      "rcon_host": {
        "description": "RCON 主机地址",
        "hint": "服务器与本机同机时填 127.0.0.1",
        "type": "string",
        "default": "127.0.0.1"
      },
      "rcon_port": {
        "description": "RCON 端口",
        "hint": "与 server.properties 中的 rcon.port 一致（默认 25575）",
        "type": "int",
        "default": 25575
      },
      "rcon_password": {
        "description": "RCON 密码",
        "hint": "与 server.properties 中的 rcon.password 一致；以明文保存在插件配置中，请勿泄露",
        "type": "string",
        "default": ""
      },
      "rcon_timeout": {
        "description": "RCON 超时（秒）",
        "hint": "单条命令的最长等待时间，0.1 ~ 300",
        "type": "float",
        "default": 5
      },
      "rcon_end_mode": {
        "description": "响应结束判定方式",
        "hint": "sentinel（默认，推荐）= 结束哨兵：发完命令后紧跟一条无害探测命令，用它的响应当可靠结束边界（要求「命令响应 → 哨兵」按序到达，顺序不成立按结果未知拒绝），慢分包/半包停顿都不会误判；idle = 静默窗口降级模式，只靠「多久没有新包」猜结束，兼容性更好但长分包响应可能被截断。连续多次无法确认边界时插件会自动降级为 idle，并在设置页常驻显示「已自动降级」、可点「重建 RCON 连接」恢复。除非服务端完全不回应探测命令，否则不要改成 idle。",
        "type": "string",
        "default": "sentinel"
      },
      "rcon_probe_command": {
        "description": "结束哨兵用的探测命令",
        "hint": "留空（默认）即用空命令——服务端照样会回一个响应，且没有任何副作用。若你的服务端对空命令报错刷屏，可填一条绝对安全的只读命令（例如 list）。",
        "type": "string",
        "default": ""
      },
      "rcon_idle_probe": {
        "description": "静默窗口（秒，仅 idle 降级模式生效）",
        "hint": "收到响应包后再等这么久没有新包才认为读完。0.05 ~ 5。网络延迟大 / 服务端分包间隔长就调大；sentinel 模式下本项不生效。",
        "type": "float",
        "default": 0.5
      }
    }
  },
  "permission": {
    "description": "② 权限与安全",
    "hint": "命令工具（执行指令 / 发物品 / 广播）的白名单与黑名单、被拦截时的「终局化」处理（前置提醒 + 会话闩锁），以及封禁时的默认理由。",
    "type": "object",
    "items": {
      "admin_ids": {
        "description": "管理员 ID 列表",
        "hint": "拥有全部权限（含危险命令）的用户 ID；群聊里填群号或 QQ 号，WebChat 里填昵称。例如 [\"Steve\", \"123456789\"]",
        "type": "list",
        "default": []
      },
      "danger_command_policy": {
        "description": "命令工具白名单 / 黑名单",
        "hint": "只管「命令工具」：mc_execute_command（执行指令）、mc_give_item（发物品）、mc_broadcast（广播）；喊话、状态、查询、绑定这类插件自带功能不受本策略影响（想限制喊话请改 say_command_public）。whitelist=白名单（默认）：非管理员完全不能使用口头命令工具；blacklist=黑名单：所有玩家都能使用命令工具，但危险命令（stop/op/ban/kick/whitelist…）与权限等级 3/4 的服务器管理命令仍仅管理员可用",
        "type": "string",
        "options": [
          "whitelist",
          "blacklist"
        ],
        "default": "whitelist"
      },
      "permission_hint_injection": {
        "description": "权限前置提醒（注入本轮对话）",
        "hint": "开启后，非管理员发起对话时会在这轮的用户消息里追加一段说明（不改写系统提示词，避免含请求者 ID 的动态内容破坏提示词缓存命中）：哪些命令工具会被直接拒绝、有哪些替代方式。比事后拦截更早一步，从源头减少「明知不可为而硬试」。关闭则只在调用被拦时才告知。",
        "type": "bool",
        "default": true
      },
      "permission_latch": {
        "description": "拦截后会话闩锁",
        "hint": "开启后，同一会话内一旦有命令被权限闸门拦下，有效期内后续命令类工具调用会被直接短路（不再触达闸门与服务器），并提示 AI 立刻停手。可有效防止 AI「换个参数继续硬试」。",
        "type": "bool",
        "default": true
      },
      "permission_latch_ttl": {
        "description": "闩锁有效期（秒）",
        "hint": "被拦后闩锁保持多久，默认 300 秒（5 分钟），到期自动解除。",
        "type": "int",
        "default": 300
      },
      "ban_default_reason": {
        "description": "封禁默认理由",
        "hint": "mcs 封禁 未附带理由时使用的预设文案",
        "type": "string",
        "default": "违反服务器规则，由管理员封禁"
      }
    }
  },
  "notify": {
    "description": "③ 事件播报（游戏 → 会话）",
    "hint": "把服务器里发生的事推送到指定会话。总开关 + 类型开关 + 播报目标三者齐备才会推送。",
    "type": "object",
    "items": {
      "enable_event_listener": {
        "description": "服务器事件转发",
        "hint": "监听服务器日志并记录最近的指定事件，并转发到指定会话（目标见下方 notify_targets）；关闭后下方各类型开关都不生效",
        "type": "bool",
        "default": false
      },
      "notify_join_leave": {
        "description": "播报：玩家加入 / 离开",
        "hint": "玩家进出服务器时推送到播报目标",
        "type": "bool",
        "default": true
      },
      "notify_death": {
        "description": "播报：玩家死亡",
        "hint": "玩家死亡时推送到播报目标",
        "type": "bool",
        "default": true
      },
      "notify_advancement": {
        "description": "播报：成就 / 进度",
        "hint": "玩家解锁成就、完成挑战或达成目标时推送",
        "type": "bool",
        "default": true
      },
      "notify_command": {
        "description": "播报：玩家指令调用",
        "hint": "玩家在游戏内执行指令时推送。原版服务端可拿到指令原文（如 /gamemode）；少数整合包只在日志里写指令反馈文案，此时播报该文案。插件自身经 RCON 下发的指令不计入；完全无反馈的静默指令也不会出现",
        "type": "bool",
        "default": true
      },
      "notify_chat": {
        "description": "播报：服务器聊天",
        "hint": "服务器内聊天内容实时转发到播报目标",
        "type": "bool",
        "default": false
      },
      "notify_targets": {
        "description": "播报目标会话",
        "hint": "格式：平台ID:消息类型:会话ID（如 aiocqhttp:FriendMessage:123456789）；WebUI 设置页提供「选类型 + 填号码」选择器自动生成；也支持简写 私聊:123456789 / 群聊:987654321（缺平台 ID 自动补全）；留空则不播报",
        "type": "list",
        "default": []
      },
      "notify_target_events": {
        "description": "各会话的播报内容（WebUI 维护）",
        "hint": "由 WebUI「播报目标」列表中点击会话展开的子菜单维护：{会话标识: [事件组]}，事件组取 join_leave / chat / death / advancement / command；未列出的会话默认全开。推送需 总开关 + 类型开关 + 目标 + 该会话勾选的事件 四者齐备。",
        "type": "dict",
        "items": {},
        "invisible": true,
        "default": {}
      }
    }
  },
  "bridge": {
    "description": "④ 聊天桥接（游戏 → 群聊）",
    "hint": "玩家在游戏内发送带触发符号的消息即可转发到群聊；全服可见，便于大家知道消息去向。",
    "type": "object",
    "items": {
      "chat_bridge_enabled": {
        "description": "桥接总开关",
        "hint": "开启后游戏内发送带符号的聊天（如 !123）即转发到指定会话",
        "type": "bool",
        "default": false
      },
      "chat_bridge_prefix": {
        "description": "触发符号（可多个，用 | 分隔）",
        "hint": "默认「!」：游戏内直接打 !123 或全角 ！123（中间无需空格），自动兼容全角/半角，大小写是否敏感见「区分大小写」；留空 = 转发全部聊天",
        "type": "string",
        "default": "!"
      },
      "chat_bridge_targets": {
        "description": "桥接目标会话",
        "hint": "格式同上（平台ID:消息类型:会话ID，支持 私聊:123456789 / 群聊:987654321 简写）；留空则沿用「播报目标会话」。推荐在目标会话里发 mcs 桥接 自动绑定",
        "type": "list",
        "default": []
      },
      "chat_bridge_format": {
        "description": "群内显示模板",
        "hint": "占位符：{player}=玩家名、{message}=正文；例如 [MC] {player}：{message}",
        "type": "string",
        "default": "[MC] {player}：{message}"
      },
      "chat_bridge_receipt": {
        "description": "游戏内转发回执",
        "hint": "玩家聊天转发至群聊成功后，将以仅该玩家可见的形式在游戏内发送一条确认信息",
        "type": "bool",
        "default": false
      },
      "chat_bridge_case_sensitive": {
        "description": "区分大小写",
        "hint": "关闭（默认）时符号大小写不敏感，Q 与 q 等价；开启后必须大小写完全一致才算命中。全角/半角始终兼容，这一项只影响英文字母的大小写",
        "type": "bool",
        "default": false
      }
    }
  },
  "commands": {
    "description": "⑤ 外部平台指令 · mcs 指令组",
    "hint": "AstrBot 聊天平台指令（不是在游戏里输入）；前缀跟随 AstrBot 唤醒词设置，例如唤醒词为 / 时即 /mcs 帮助。逐条控制 mcs 指令组，关闭后该指令对所有人不可用；标注（管理员）的仍受管理员列表保护。",
    "type": "object",
    "items": {
      "enable_bind_command": {
        "description": "绑定",
        "hint": "mcs 绑定 <MC玩家ID> / mcs 解绑 / mcs 查询；未指名玩家的任务会自动使用已绑定的游戏名",
        "type": "bool",
        "default": true
      },
      "enable_say_command": {
        "description": "喊话",
        "hint": "mcs 喊话 <内容>：把群内消息转发到服务器聊天栏，自动带上发送者昵称",
        "type": "bool",
        "default": true
      },
      "say_command_public": {
        "description": "喊话对全员开放",
        "hint": "打开 = 所有群成员可喊话；关闭 = 仅管理员可喊话",
        "type": "bool",
        "default": true
      },
      "enable_status_command": {
        "description": "状态",
        "hint": "mcs 状态：查询 mspt / tps / 在线人数 / 运行时长 / 内存 / CPU（TPS 依赖 Spark 等模组，内存 CPU 需同机部署）",
        "type": "bool",
        "default": true
      },
      "enable_kick_command": {
        "description": "踢人（管理员）",
        "hint": "mcs 踢人 <MC玩家ID> [理由]",
        "type": "bool",
        "default": true
      },
      "enable_ban_command": {
        "description": "封禁（管理员）",
        "hint": "mcs 封禁 <MC玩家ID> [理由]",
        "type": "bool",
        "default": true
      },
      "enable_unban_command": {
        "description": "解封（管理员）",
        "hint": "mcs 解封 <MC玩家ID>",
        "type": "bool",
        "default": true
      },
      "enable_banlist_command": {
        "description": "封禁列表（管理员）",
        "hint": "mcs 封禁列表：查看当前被封禁的玩家",
        "type": "bool",
        "default": true
      },
      "enable_help_command": {
        "description": "帮助",
        "hint": "mcs 帮助：查看指令组全部用法，管理员专属指令会标注（管理员）",
        "type": "bool",
        "default": true
      },
      "enable_title_command": {
        "description": "全屏喊话（管理员）",
        "hint": "mcs 全屏喊话 <内容>：用 title @a title 全屏显示给所有人",
        "type": "bool",
        "default": true
      }
    }
  },
  "tools": {
    "description": "⑥ AI 工具（LLM 可调用）",
    "hint": "控制 AI（LLM）能调用哪些 Minecraft 工具；关闭后对应能力立即不可用。",
    "type": "object",
    "items": {
      "enable_mc_execute_command": {
        "description": "万能命令执行",
        "hint": "mc_execute_command：执行任意一条服务器命令",
        "type": "bool",
        "default": true
      },
      "enable_mc_list_players": {
        "description": "在线玩家查询",
        "hint": "mc_list_players：查询当前在线玩家与人数",
        "type": "bool",
        "default": true
      },
      "enable_mc_server_status": {
        "description": "服务器状态查询",
        "hint": "mc_server_status：版本、在线人数、游戏内时间等",
        "type": "bool",
        "default": true
      },
      "enable_mc_connection_status": {
        "description": "连接自检",
        "hint": "mc_connection_status：测试与服务器的 RCON 连接是否正常",
        "type": "bool",
        "default": true
      },
      "enable_mc_broadcast": {
        "description": "服务器广播",
        "hint": "mc_broadcast：向服务器内所有玩家广播消息",
        "type": "bool",
        "default": true
      },
      "enable_mc_give_item": {
        "description": "发放物品",
        "hint": "mc_give_item：给指定玩家发放物品",
        "type": "bool",
        "default": true
      },
      "enable_mc_kick": {
        "description": "踢人",
        "hint": "mc_kick：将玩家踢出服务器",
        "type": "bool",
        "default": true
      },
      "enable_mc_ban": {
        "description": "封禁",
        "hint": "mc_ban：封禁指定玩家",
        "type": "bool",
        "default": true
      },
      "enable_mc_search_item": {
        "description": "物品ID搜索",
        "hint": "mc_search_item：按中文名 / 英文名 / ID 片段搜索精确物品 ID",
        "type": "bool",
        "default": true
      },
      "enable_mc_get_recipes": {
        "description": "配方查询",
        "hint": "mc_get_recipes：正向查合成方法、反向查能造什么",
        "type": "bool",
        "default": true
      },
      "enable_mc_list_mods": {
        "description": "Mod 列表",
        "hint": "mc_list_mods：列出服务器实际安装的全部 Mod",
        "type": "bool",
        "default": true
      },
      "enable_mc_reload_plugin": {
        "description": "热重载插件",
        "hint": "mc_reload_plugin：重载插件让更新立即生效（深度清理模块缓存与 __pycache__，无需重启 AstrBot）。仅作开发/调试用：聊天界面不提供重载指令，日常重载请到 AstrBot「插件管理」页操作",
        "type": "bool",
        "default": true
      }
    }
  },
  "knowledge": {
    "description": "⑦ 词典与知识库",
    "hint": "整合包特化能力：扫描 mods 构建中英物品词典，并按整合包指纹隔离沉淀知识（换整合包不再自动切换/继承，由用户在 WebUI 知识库页手动指定预设）。",
    "type": "object",
    "items": {
      "dictionary_enabled": {
        "description": "物品词典",
        "hint": "启动时扫描 mods 目录构建中英文物品词典；关闭后物品搜索与配方查询不可用",
        "type": "bool",
        "default": true
      },
      "knowledge_enabled": {
        "description": "学习型模组知识库",
        "hint": "按整合包指纹（mod ID 集合）隔离沉淀 NBT 格式、枪械配件方案等经验",
        "type": "bool",
        "default": true
      },
      "knowledge_search_engine": {
        "description": "知识库检索引擎",
        "hint": "bm25=中文二元切分 + BM25 + 倒排索引（推荐）：字序敏感、高频套话自动降权。本机实测：26 条语料 17 组查询下，结果精确率 20.5%→58.6%（2.9 倍）、无关条目减少 82%；5000 条规模单次检索 6.3ms→2.0ms；legacy=旧版检索（中文按单字、英文按整段做子串命中），字面与历史结果完全一致但容易连锁误召回，仅建议「需要与历史检索结果完全相同」时使用。切换后存一次设置即生效，无需重载插件",
        "type": "string",
        "options": [
          "bm25",
          "legacy"
        ],
        "default": "bm25"
      },
      "enable_mc_search_knowledge": {
        "description": "知识检索工具",
        "hint": "mc_search_knowledge：让 AI 先查库再推理",
        "type": "bool",
        "default": true
      },
      "enable_mc_save_knowledge": {
        "description": "知识沉淀工具",
        "hint": "mc_save_knowledge：把推理成功的经验写入知识库",
        "type": "bool",
        "default": true
      },
      "enable_mc_correct_knowledge": {
        "description": "知识纠错工具",
        "hint": "mc_correct_knowledge：用正确方案覆盖错误条目（纠错闭环）",
        "type": "bool",
        "default": true
      },
      "knowledge_semantic_search": {
        "description": "语义增强检索（只建议大型服务器启用）",
        "hint": "开启后知识库除 BM25 字面匹配外，再走一条嵌入向量语义通道，两通道用 RRF 融合，并带相似度门槛过滤无关候选。适合条目多、且提问常用口语改写的大型服务器（如「后台连不上了」也能找到「RCON 连接失败排查」）。本机实测（438 条语料 / 24 组查询）：Hit@1 75.0%→87.5%、Hit@6 79.2%→100%（口语改写型 Hit@6 58.3%→100%），无关条目误召回归零。代价：检索时多一次嵌入调用（约 300~500ms 延迟），建库时每条约一次嵌入调用（后台分批、只算新增/改动条目）。需要先在 AstrBot「服务提供商」添加一个 Embedding 模型（推荐文本嵌入模型），否则本通道安静退化为纯 BM25。",
        "type": "bool",
        "default": false
      },
      "knowledge_rerank": {
        "description": "重排序精排（召回后加一道 Cross-Encoder 重排）",
        "hint": "开启后知识库检索升级为「召回 → 精排」两段式：先用 BM25（可叠加语义通道）召回首 20 条候选池，再交给 Rerank 模型逐条打分重排，最后取前几条。精排看的是「查询与条目的真实相关性」，能把召回到了但排不前的正确条目提上来。与「语义增强检索」相互独立：没有嵌入模型也能单独开；两者同开就是「BM25 + 向量 RRF 召回 → rerank 精排」的完整三段式。需要先在 AstrBot「服务提供商」添加一个 Rerank 模型（如 vllm_rerank / Qwen3-Reranker），否则本通道安静退化为召回顺序。代价：每次检索多一次重排 API 往返，小库（几十条）不值得开，条目多、问法杂时才划算。",
        "type": "bool",
        "default": false
      },
      "knowledge_embed_provider_id": {
        "description": "指定嵌入模型（留空 = 自动用第一个）",
        "hint": "语义增强检索用哪个嵌入模型：填 AstrBot「服务提供商」里 Embedding 模型的 ID（形如 qwen3-embed）。留空 = 使用 AstrBot 里加载的第一个嵌入模型（与旧行为一致）。配置了多个嵌入模型时用它指定；指定的模型被删除 / 停用后会安静回落到第一个，并在 WebUI 保存时提示，不会让检索失效。WebUI 设置页有下拉可直接选。",
        "type": "string",
        "default": ""
      },
      "knowledge_rerank_provider_id": {
        "description": "指定重排序模型（留空 = 自动用第一个）",
        "hint": "重排序精排用哪个 Rerank 模型：填 AstrBot「服务提供商」里 Rerank 模型的 ID（形如 qwen3-reranker）。留空 = 使用 AstrBot 里加载的第一个 Rerank 模型（与旧行为一致）。配置了多个重排序模型时用它指定；指定的模型被删除 / 停用后会安静回落到第一个，并在 WebUI 保存时提示，不会让检索失效。WebUI 设置页有下拉可直接选。",
        "type": "string",
        "default": ""
      }
    }
  },
  "appearance": {
    "description": "⑧ 外观 · 反馈与渐变颜色",
    "hint": "游戏内的署名反馈，以及喊话 / 全屏标题 / 任务输出的文字颜色与渐变。",
    "type": "object",
    "items": {
      "feedback_name": {
        "description": "反馈署名",
        "hint": "命令反馈与广播中显示的名字",
        "type": "string",
        "default": "RCON"
      },
      "feedback_tellraw": {
        "description": "游戏内任务完成后将在公屏反馈",
        "hint": "开启后，任务执行完成会在游戏公屏发送署名反馈；反馈文字颜色可在 WebUI「服务器」页的『文本颜色』卡片更改",
        "type": "bool",
        "default": true
      },
      "gradient_enabled": {
        "description": "启用渐变色（MC 1.16+）",
        "hint": "按各处「渐变锚点」逐字符渐变；起始色沿用对应单色配置，空格不参与渐变",
        "type": "bool",
        "default": false
      },
      "gradient_format": {
        "description": "渐变输出格式",
        "hint": "json=Vanilla（JSON 文本组件，推荐）｜compat_section=Vanilla 兼容（§x§R§R§G§G§B§B）｜compat_amp=Vanilla 兼容（&x&R&R&G&G&B&B）｜legacy_amp=Legacy（&#RRGGBB）",
        "type": "string",
        "options": [
          "json",
          "compat_section",
          "compat_amp",
          "legacy_amp"
        ],
        "default": "json"
      },
      "color_say": {
        "description": "聊天栏颜色（普通喊话）",
        "hint": "mcs 喊话 的聊天栏颜色。支持 hex(#FF0000) / RGB(255,0,0) / 十进制(16711680) 或色名(white/red/gold…)",
        "type": "string",
        "default": "white"
      },
      "gradient_colors_say": {
        "description": "聊天栏 · 渐变锚点（普通喊话）",
        "hint": "2 个以上锚点色，逗号分隔，如 #FFFFFF,#55FFFF",
        "type": "string",
        "default": "#FFFFFF,#55FFFF"
      },
      "color_title": {
        "description": "全屏标题颜色（全屏喊话）",
        "hint": "mcs 全屏喊话 的 title 大字颜色，格式同上",
        "type": "string",
        "default": "gold"
      },
      "gradient_colors_title": {
        "description": "全屏标题 · 渐变锚点（全屏喊话）",
        "hint": "2 个以上锚点色，逗号分隔，如 #FFAA00,#FF00FF",
        "type": "string",
        "default": "#FFAA00,#FF00FF"
      },
      "color_feedback": {
        "description": "模拟任务输出颜色",
        "hint": "署名反馈的颜色，格式同上",
        "type": "string",
        "default": "gold"
      },
      "gradient_colors_feedback": {
        "description": "模拟任务输出 · 渐变锚点",
        "hint": "2 个以上锚点色，逗号分隔，如 #FFAA00,#55FF55",
        "type": "string",
        "default": "#FFAA00,#55FF55"
      }
    }
  },
  "workflow": {
    "description": "⑨ 多 Agent 工作流",
    "hint": "复杂任务自动走「分类 → 模板判断 → 模板工程师 → 实现 → 纠错」流水线。各 Agent 的 Provider 留空则回退：主 Provider → 会话默认 → 第一个可用。",
    "type": "object",
    "items": {
      "agent_workflow_enabled": {
        "description": "工作流总开关",
        "hint": "开启后复杂模组任务自动交由多 Agent 流水线处理；关闭后 mc_workflow 不再执行，复杂任务会退化为单工具分步模式（AI 自行查库/查 ID/查配方后下发指令）",
        "type": "bool",
        "default": true
      },
      "enable_mc_workflow": {
        "description": "mc_workflow 工具",
        "hint": "是否把 mc_workflow 暴露给 LLM（复杂任务总入口）。关闭后模型仍会看到该工具，但调用时会收到「请改用 mc_search_knowledge / mc_search_item / mc_get_recipes / mc_execute_command」的导航提示，不会执行流水线",
        "type": "bool",
        "default": true
      },
      "llm_provider_id": {
        "description": "主 Provider",
        "hint": "流水线的回退 Provider；留空则用会话默认 Provider",
        "type": "string",
        "_special": "select_provider",
        "default": ""
      },
      "agent_classifier_provider_id": {
        "description": "Agent#1 分类器 Provider",
        "hint": "判断任务简单 / 复杂；留空回退主 Provider",
        "type": "string",
        "_special": "select_provider",
        "default": ""
      },
      "agent_judge_provider_id": {
        "description": "Agent#2 模板判断器 Provider",
        "hint": "判断知识库模板是否够用；留空回退主 Provider",
        "type": "string",
        "_special": "select_provider",
        "default": ""
      },
      "agent_engineer_provider_id": {
        "description": "Agent#3 模板工程师 Provider",
        "hint": "前瞻建库、生成操作手册型模板；留空回退主 Provider",
        "type": "string",
        "_special": "select_provider",
        "default": ""
      },
      "agent_implementer_provider_id": {
        "description": "Agent#4 实现器 Provider",
        "hint": "套模板构造命令，建议用最强模型保证精度；留空回退主 Provider",
        "type": "string",
        "_special": "select_provider",
        "default": ""
      },
      "agent_corrector_provider_id": {
        "description": "Agent#5 纠错器 Provider",
        "hint": "分析失败根因并修正模板；留空回退主 Provider",
        "type": "string",
        "_special": "select_provider",
        "default": ""
      },
      "agent_max_implement_rounds": {
        "description": "实现器最大尝试轮数",
        "hint": "单次任务内实现器最多尝试几轮（含纠错后重试）",
        "type": "int",
        "default": 3,
        "slider": {
          "min": 1,
          "max": 10,
          "step": 1
        }
      },
      "agent_max_correct_rounds": {
        "description": "纠错循环最大轮数",
        "hint": "实现失败后最多进入几轮「纠错 → 重试」",
        "type": "int",
        "default": 2,
        "slider": {
          "min": 0,
          "max": 10,
          "step": 1
        }
      }
    }
  },
  "prompts": {
    "description": "⑩ 多 Agent 提示词",
    "hint": "多 Agent 流水线五个角色的系统提示词，可在此查看与编辑（与插件 WebUI「提示词」页共用同一份配置）。五个角色依次为：分类器 → 模板判断器 → 模板工程师 → 实现器 → 纠错器。",
    "type": "object",
    "items": {
      "agent_prompt_classifier": {
        "description": "Agent#1 决策分类器 · 系统提示词",
        "hint": "判断任务属于 simple（原版命令）/ complex（模组任务），并做玩家守门判定（requires_player / target / player_name）。输出严格 JSON · 单轮调用。留空或保持与内置一致＝跟随内置默认（推荐，插件升级时自动获得提示词改进）；改动后即时生效、无需重启。注意：末尾「输出要求（严格 JSON）」的字段约定不要删改，否则流水线无法解析该 Agent 的返回。",
        "type": "text",
        "editor_mode": true,
        "editor_language": "markdown",
        "default": "你是一个 Minecraft 服务器指令任务的分类器。你的职责是判断用户请求属于哪一类，并给出建议。\n\n分类标准：\n- \"simple\"：仅用原版 Minecraft 命令即可完成的任务。例如：发原版物品（diamond_sword/diamond/oak_log 等无模组前缀的 ID）、调整时间天气（time/weather）、传送/召唤原版实体、给予原版效果、踢人封禁、普通查询等。不需要查阅模组知识库，不需要复杂 NBT。\n- \"complex\"：涉及整合包模组内容的任务。例如：带模组 ID 前缀的物品、模组枪械满配/配件方案、模组任务/进度、数值计算（配比/产量/耗材等）、复杂 NBT 结构（附魔、枪械附件 Attachments、模组方块实体等）、多步骤操作、需要查配方/查模组机制的任务。\n\n判断规则：\n1. 只要请求中出现模组 ID 前缀、或需要模组知识才能构造的任务，一律判为 complex。\n2. 原版物品（diamond、iron_sword、oak_log、diamond_sword 等）判为 simple。\n3. 拿不准时判为 complex（宁可多走一步知识库，不要出错）。\n4. 一次性多条请求（如\"给每人发钻石剑并广播\"）按最复杂的那个分类。\n\n【玩家目标判定（重要，用于决策AI玩家守门）】\n- requires_player：任务是否针对玩家实体执行（give/kick/ban/tp/kill/enchant 等涉及具体玩家的操作）。改时间天气、广播、查状态、召唤实体等不针对玩家的任务为 false。\n- target（requires_player=true 时必填）：\n  - \"self\"：请求指向当前用户自己（如「给我发一把钻石剑」「送我一把枪」）\n  - \"all\"：请求面向全体玩家（如「给所有人发一组钻石」「给每人发」）\n  - \"named\"：请求明确指名了具体玩家（如「给 Steve 发钻石剑」「给 Alex 满配」）\n  - \"none\"：不针对玩家（requires_player=false 时用）\n- player_name：target=\"named\" 时填请求中出现的玩家名原样（如 \"Steve\"、\"Alex\"，可能是昵称或真实名）；target=\"all\" 时填 \"@a\"；其余填空字符串。\n- 不要把「所有玩家/全体/每人」判成 named；「给每人发一组钻石」→ requires_player=true, target=\"all\", player_name=\"@a\"。\n\n输出要求（严格 JSON，不要输出任何多余文字）：\n{\"type\": \"simple\" 或 \"complex\", \"requires_player\": true 或 false, \"target\": \"none/self/all/named\", \"player_name\": \"指名玩家名或@a或空\", \"reason\": \"一句话判断理由\", \"commands\": [\"simple 时给出可直接执行的命令，不含开头斜杠；complex 时为空数组\"]}"
      },
      "agent_prompt_judge": {
        "description": "Agent#2 模板判断器 · 系统提示词",
        "hint": "判断知识库检索结果里的 template 模板是否足以直接完成任务，不足时说明缺口。输出严格 JSON · 单轮调用。留空或保持与内置一致＝跟随内置默认（推荐，插件升级时自动获得提示词改进）；改动后即时生效、无需重启。注意：末尾「输出要求（严格 JSON）」的字段约定不要删改，否则流水线无法解析该 Agent 的返回。",
        "type": "text",
        "editor_mode": true,
        "editor_language": "markdown",
        "default": "你是 Minecraft 知识库模板的「可用性判断器」。给定一个用户请求和知识库检索结果（已按相关度排序，含 template 模板类条目与 instance 实例类条目），判断现有模板能否直接支撑完成该任务。\n\n判断标准：\n- sufficient=true 的条件：检索结果中存在 template 类条目，且其内容包含【完整可执行的操作步骤 / 命令模板 / 关键数值表 / 查询路径】，足以直接据此构造出答案，无需再自行探索。\n- sufficient=false 的条件：检索结果只有 instance 实例（只针对某一具体物品，不能泛化到请求的物品种类）；或 template 内容只是概念描述、缺少关键数据/步骤/数值；或完全没有相关条目。\n\n注意：\n- instance 条目（如\"某把枪的具体方案\"）只能解决它自己，不能算模板；如果请求的物品与 instance 不同类，则视为不足。\n- 如果存在可泛化的 template（如\"枪械满配通用流程\"），且请求的物品种类在该模板覆盖范围内，则 sufficient=true。\n- 输出 JSON 时在 \"use_templates\" 里列出判断所依据的模板 topic。\n\n输出要求（严格 JSON）：\n{\"sufficient\": true 或 false, \"gap\": \"不足时说明缺什么；足够时填空字符串\", \"use_templates\": [\"所用模板topic列表\"]}"
      },
      "agent_prompt_engineer": {
        "description": "Agent#3 模板工程师 · 系统提示词",
        "hint": "前瞻建库：为当前请求沉淀「操作手册型 + 举一反三」的可复用模板。输出严格 JSON · max_tokens 4000 / 180s。留空或保持与内置一致＝跟随内置默认（推荐，插件升级时自动获得提示词改进）；改动后即时生效、无需重启。注意：末尾「输出要求（严格 JSON）」的字段约定不要删改，否则流水线无法解析该 Agent 的返回。",
        "type": "text",
        "editor_mode": true,
        "editor_language": "markdown",
        "default": "你是 Minecraft 整合包知识库的「模板工程师」。职责：为当前请求创建或完善【可复用的、前瞻性的】知识库模板（template），让未来的同类任务可以直接套用，不再需要反复探索。\n\n输入信息：\n- 用户请求（当前要解决的任务）\n- 知识库现有相关条目（可能为空或只有实例）\n- 可用的物品/配方词典信息（物品 ID）\n\n模板编写要求：\n1. 【操作手册型】：内容必须包含具体可执行的步骤、查询路径（如\"读取 mod jar 内 data/tac/guns/{枪id}.json 的 slots 字段\"这种可复现的路径）、命令模板、关键数值表、易踩的坑。绝不允许空泛概念。\n2. 【前瞻性/举一反三】：不只解决当前这一个请求。例如当前任务是\"满配某把枪\"，就要把该模组体系下所有常见枪械的槽位规律、配件对照表、通用组装命令模板都写进去，使未来任意一把枪（乃至同类物品）都能直接套用。\n3. 【通用可迁移】：总结的方法不局限当前模组场景，对其它复杂模组任务（任务链、机械、电力等）的建库方法也要有普适性——模板里要体现\"如何快速定位模组数据/配方/机制\"的通用方法论。\n4. 【准确具体】：物品 ID、NBT 键名、数值必须精确；不确定的字段标注\"需验证\"。\n5. 模板条目尽量自包含：单个 template 条目应能在不依赖其它探索的情况下指导完成任务。\n\n输出要求（严格 JSON）：\n{\"templates\": [{\"topic\": \"条目标题(含模组标识，如 'tacz 枪械满配通用流程')\", \"content\": \"完整模板正文\", \"mod\": \"模组ID(如 tac/create，不确定留空)\"}], \"summary\": \"一句话总结建了哪些模板及如何覆盖未来场景\"}"
      },
      "agent_prompt_implementer": {
        "description": "Agent#4 实现器 · 系统提示词",
        "hint": "套用知识库模板把请求转成精确可执行的命令，并判定执行结果、失败重试。输出严格 JSON · max_tokens 2500 / 180s。留空或保持与内置一致＝跟随内置默认（推荐，插件升级时自动获得提示词改进）；改动后即时生效、无需重启。注意：末尾「输出要求（严格 JSON）」的字段约定不要删改，否则流水线无法解析该 Agent 的返回。",
        "type": "text",
        "editor_mode": true,
        "editor_language": "markdown",
        "default": "你是 Minecraft 服务器指令的「执行专家」。基于提供给你的知识库模板/检索信息，把用户请求转化为精确可执行的 Minecraft 命令。你只负责构造命令并判断执行结果，实际执行由系统完成。\n\n输入信息（JSON）：\n- request: 用户请求原文\n- player: 目标玩家名（可能为空）\n- online_players: 当前服务器真实在线玩家名单（英文逗号分隔；目标玩家名必须取自这里）\n- knowledge: 知识库检索结果（模板/实例条目全文，最相关在前）\n- previous_results: 之前的执行结果（首次为空；后续轮次为上次每条命令的返回，用于修正）\n- retry_hint: 纠错专家给出的重试提示（可能为空）\n\n构造规则：\n1. 命令不含开头斜杠。\n2. 严格套用知识库模板中的 NBT 格式、槽位键名、物品 ID、数值；不要自行发明格式。\n3. 模板没有覆盖的部分，基于 Minecraft 通用命令知识构造，并尽量保守（以一次成功为目标）。\n4. 涉及模组物品时，物品 ID 必须带模组前缀（如 tac:hk_mp5a5）；不确定 ID 时用 {item_id_unknown: \"对物品的用途描述\"} 标注，不要乱猜。\n5. 一次任务可以输出多条命令（如发枪+发弹药+反馈）。\n6. 每条命令附一条自然语言反馈文案（用于游戏内展示给玩家，不要带头衔前缀，贴合用户意图）。\n7. 目标玩家名必须使用 online_players 中列出的真实游戏名（区分大小写），禁止使用聊天昵称、缩写或推测名。若 player 不在在线名单中，取与之最接近的在线玩家真实名；若在线名单为空，直接输出 success=false 并说明「当前无玩家在线」。\n\n执行结果判定：\n- 系统会回传每条命令的实际执行反馈。判断成功或失败（如 Gave 确认、命令不存在、语法错误、物品ID无效、NBT 解析失败等）。\n- 若失败，根据反馈修正命令后再次输出；连续失败不要重复同样的错误，尝试其它合理写法或换一种实现方式。\n- 全部成功即结束；确实无法完成时，输出 success=false 并说明原因。\n\n输出要求（严格 JSON）：\n{\"commands\": [{\"command\": \"give ...\", \"feedback\": \"游戏内自然语言反馈文案\"}], \"success\": true 或 false, \"reasoning\": \"简要说明构造思路或修正思路\"}"
      },
      "agent_prompt_corrector": {
        "description": "Agent#5 纠错器 · 系统提示词",
        "hint": "实现器多轮失败后分析根因、修正模板并给出给实现器的重试指引。输出严格 JSON · max_tokens 4000 / 180s。留空或保持与内置一致＝跟随内置默认（推荐，插件升级时自动获得提示词改进）；改动后即时生效、无需重启。注意：末尾「输出要求（严格 JSON）」的字段约定不要删改，否则流水线无法解析该 Agent 的返回。",
        "type": "text",
        "editor_mode": true,
        "editor_language": "markdown",
        "default": "你是 Minecraft 知识库模板的「纠错专家」。实现器多次尝试后仍失败，需要你分析根因并修正模板，把这次失败的教训固化下来，防止未来重蹈覆辙。\n\n输入信息（JSON）：\n- request: 用户请求原文\n- knowledge: 执行时使用的知识库模板/检索内容（全文）\n- failures: 多轮执行失败的详细信息（每条命令 + 服务器返回/报错）\n- current_templates: 当前需要修正的模板 topic 与内容\n\n你的职责：\n1. 【根因分析】：判断失败原因属于哪类——命令语法错误、NBT 格式错误（键名/大小写/嵌套）、物品 ID 无效、槽位不匹配、缺前置步骤、方法本身不可行等。\n2. 【修正模板】：找出导致失败的模板内容错误或缺失，给出修正后的【完整】模板正文（在原文基础上修改/补充，不是只写差异）。\n3. 【总结教训】：用一句话凝练这次教训（如\"消音器是 Barrel 槽，pistol_silencer 是手枪专用装不上\"），作为注意事项写进模板。\n4. 【重试提示】：给实现器一句明确的修正指引（如\"枪口槽改用 muzzle_brake\"）。\n\n输出要求（严格 JSON）：\n{\"corrections\": [{\"topic\": \"要修正的模板topic\", \"new_content\": \"修正后的完整模板正文\"}], \"lessons\": \"一句话教训\", \"retry_hint\": \"给实现器的重试指引\"}"
      }
    }
  }
}