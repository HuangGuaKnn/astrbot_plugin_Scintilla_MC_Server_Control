# 更新日志

本项目的所有重要变更都记录在这里。格式参考
[Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循语义化版本。

> **写作约定**：每个版本小节的第一行请用一行引用块写「本版一句话」摘要
> （`> 一句话：…`）。发布 Release 时会自动把小节正文当作说明，
> 这句摘要就会出现在最外层，访客不展开细节也能看懂这一版干了什么。

## [v0.23.1] - 2026-09-22

> 一句话：**版本号重发** —— v0.23.0 在发布流程中被提前提交到插件市场、该版本号随即被占用（市场规则：版本号一经使用即不可复用，即使撤回或删除也不行），本次**代码与 v0.23.0 完全一致**，仅提升版本号以通过市场校验。

### 说明

- **与 v0.23.0 没有任何功能差异**：v0.23.0 才是真正的功能版本（RCON 卡片进阶项折叠 + 两处「配置根本没存住」修复 + 玩家绑定补测，详见下一节）。
  从 AstrBot 插件市场安装请用本版；从 GitHub 直接下载 zip 的话，两者内容等价。
- 随本版一并带上的仓库侧调整：
  - 发布说明瘦身 —— v0.22.1 ~ v0.22.11 十个预发布版本的改动合并为一句话摘要，
    完整提交与文件差异改由 Release 页末尾的 **Full changes** 链接给出（原本逐版全文附上，太长）；
  - `release.yml` 新增一步：发版时自动查出**上一个正式版**（只认 `prerelease == false` 的真实发布状态，
    不靠 CHANGELOG 标题推断 —— v0.22.3 / v0.22.4 没写「预发布」但从未发过 Release），
    并把 `compare/<上一个正式版>...<本版>` 链接追加到发布说明末尾；该步失败不阻断发布。

## [v0.23.0] - 2026-09-22

> 一句话：1.20.1 实机实测后的正式版 —— 设置页 RCON 卡片的进阶调参收进折叠区（943px → 519px），修掉两处「界面看着正常、配置其实没存住」的老洞（保存误报「插件未重载」、各会话播报内容一重载就恢复全开），并为玩家绑定补上零覆盖的测试与一处浅拷贝缺陷。

### 修复

- **设置页保存长期误报「⚠ 有 2 项后端未接受（item_syntax_override、server_version_override）：插件可能未重载」**
  （v0.22.7 引入，v0.23.0 修）：`core/web_api.py` 的 `_validate_settings` 白名单漏了这两个键 ——
  前端每次保存都会提交它们，后端认不出 → 一律回 `ignored`，界面据此提示「插件未重载，请重载后重新保存」。
  **重载当然无效**（缺的是代码，不是加载状态），跟着提示反复重载也没用；
  更隐蔽的是这两个值**从未真正落盘**：在设置页填过的服务端版本 / 语法世代，保存后即被丢弃
  （改走 AstrBot 原生配置页才存得下）。
  现已补齐白名单；`item_syntax_override` 的合法取值直接引用 `core/version_caps.py` 的
  `ITEM_SYNTAX_CHOICES`，不再手抄第三份清单。
- **服务器事件转发：各会话的播报内容一重载就全部恢复默认「全开」**（v0.17.0 引入，v0.23.0 修）：
  `notify_target_events` 在 `_conf_schema.json` 里被声明成 `"type": "object"` + 空 `items`，
  但 AstrBot 配置系统里 `object` 是「按 `items` 子键校验的**固定结构**」，自由映射必须写
  `type: "dict"`。于是 `AstrBotConfig` 每次加载（= 插件每次重载）都会跑 `check_config_integrity`，
  把用户自己加的会话键当成「过期条目」删掉再写回 `{}`：
  `[INFO] [config.astrbot_config:245] Config key removed: notify.notify_target_events.<会话标识>`。
  症状：给某个群聊静音、另一个私聊只留「指令调用」，重载插件后全部恢复默认全开。
  它一直没被发现，是因为**内存里完全正常**（保存后即时生效）；只有重载 / 重启才现形。
  已改为 `type: "dict"`（与 AstrBot 官方 `custom_headers` 等自由映射同款写法）。
- 新增守卫 `tests/test_config_persistence_contract.py`：schema 契约（`object` 必须带非空 `items`）
  ＋ 真 `AstrBotConfig` 逐键往返（88 个键写入探针 → 重新加载 → 逐键比对）
  ＋ 定点复现主人那次的场景。把 schema 改回 `object` 即 4 项失败。
- 新增守卫 `tests/test_settings_whitelist_contract.py`（三层：静态比对「前端提交键 ⊆ 后端接受键」、
  真后端 `_validate_settings` 接受与拒收行为、真浏览器点「保存全部设置」截获真实载荷喂真后端）。
  另有一套 mock 后端 UI 测试（`ui_version_override_check.py`）此前之所以全绿，
  正是因为**它跑的是假后端**、压根不经过真实校验 —— 同类漏项只能靠这个新守卫兜住。

### 变更

- 设置页 **RCON 卡**：地址 / 端口 / 密码之后的调参（超时、响应结束判定、哨兵与静默窗口、版本与语法）
  统一收进「进阶设置」折叠区，**默认收起**，点标题按需展开 —— 卡片高度 943px → 519px。
  收起态在标题旁留一枚摘要（`当前：超时 5s · sentinel（结束哨兵）`），
  异地模式漏填服务端版本时变警示色并点名要补什么；异地模式提示里也有可点的展开入口。
  承载安全警示的**版本能力行、运行状态行**与「重建 RCON 连接」按钮**留在折叠区外** ——
  折叠起来等于把「版本未知 → 带数据的请求会被拒绝」「响应边界未确认 / 已自动降级」的警示藏掉。


---

> **上一个正式版 v0.22.1 以来的全部改动（一句话版）**：v0.22.2 ~ v0.22.11 这十个预发布版本，把命令结果判定全面改为 fail-closed —— 收口了 RCON 响应边界（哨兵乱序 / 延迟分包截断 / 超时误报「已执行」）、缺 status 时的假成功、非幂等命令的重复副作用，以及权限黑名单的 `execute run` 包装绕过；同区间还加入了版本能力矩阵与异地 RCON 模式。完整提交与文件差异见本页末尾的 **Full changes** 链接。

## [v0.22.11] - 2026-09-22（预发布）

> 一句话：给「数值计算」补上一道**确定性硬路由**（不再只靠分类器提示词 —— 配比/产量/耗材类请求命中特征词即强制走复杂流水线），并收口两处措辞：停手回执补上「也不会自动重试」，全插件「数值计算」统一写成「数值计算（配比/产量/耗材等）」。

> 本版为**预发布（pre-release）**：仍未在 Vanilla / Paper / Fabric 真实服务端做矩阵测试。
> 求稳请继续用 v0.22.1。

### 新增

#### 一、数值计算补确定性路由守门（v0.22.10 核验 · GPT「额外发现」）

`core/command_result.py` 新增 `_COMPLEX_NUMERIC_MARKERS` 与
`looks_like_numeric_request()`，并接入既有的 `looks_like_complex_request()`：

```python
_COMPLEX_NUMERIC_MARKERS = (
    "数值计算", "配比", "产量", "耗材", "材料需求", "需要多少",
    "每分钟", "每秒产出", "机器效率",
)
```

**为什么必须加**：分类器提示词只能让模型**倾向**判 complex —— 模型一旦返回
`simple`，提示词毫无约束力，而 simple 是全插件防护最弱的一条路（没有输出校验、
没有纠错、没有到账核验）。本守门与「附魔 / NBT / 组件」等既有特征同源，
**只做升级（simple → complex），绝不反向降级**。

实测口径：`算一下蒸汽机需要多少铜`、`蒸汽机每分钟能产多少`、`这条产线的产量是多少`
一律强制转 complex；`把时间设成白天`、`给 Steve 一把钻石剑`、`广播一句 hi`
等不含特征词的普通请求**不受影响**（不白烧 token）。

### 修复

#### 二、停手回执补上「也不会自动重试」（v0.22.10 核验 · GPT 裁决 2）

结构异常回执原文第 3 句只说「本次没有继续发送命令」——这是**事实陈述**，
却少了「不会自动重试」这层**机制承诺**，用户可能误以为后台仍在悄悄重试。
现与 `unknown / inferred_success / dispatched_unconfirmed` 的既有回执口径对齐：

```text
工作流已暂停：内部执行状态异常，无法安全确认命令对应关系。
（命令与执行报告数量不一致：2 条命令 / 1 份报告）
为避免重复执行或漏发，本次没有继续发送命令，也不会自动重试。
请先在游戏内确认实际结果，再决定是否重新发起；若持续出现，请反馈此提示。
```

### 文档

#### 三、「数值计算」文案统一为「数值计算（配比/产量/耗材等）」

工具描述（`main.py` 6 处）、插件 WebUI（`pages/mc_control/index.html` 3 处）、
插件帮助（`metadata.yaml`）与分类器提示词（`core/agent_prompts.py`、
`_conf_schema.json` 默认值）统一带上括号说明，用户与 LLM 看到的是同一套口径。

### 测试

* 新增「三·五、数值计算：提示词之外的确定性守门」一组（15 项），
  `tests/test_complex_routing.py` 现共 **45 项**全绿；
* 结构异常回执新增 1 条断言（写明不会自动重试），
  `tests/test_v02210_label_and_contract.py` 全绿；
* 全量：`test_*.py` **26 / 26 通过**、`ui_*.py` **10 / 10 通过**、`compileall` 通过。

## [v0.22.10] - 2026-09-22（预发布）

> 一句话：把「同一份判定」的两套说法收成一套 —— 回执标签统一走 `STATUS_LABEL`（不再自己一套 `OK` / `FAIL`），命令记账改成 **fail-closed**（报告与命令数量对不上就停手问人，不再靠 `zip()` 静默截断），并且**记账判据与熔断判据同源**（只认 `_status_of() == "success"`）。

> 本版为**预发布（pre-release）**：仍未在 Vanilla / Paper / Fabric 真实服务端做矩阵测试。
> 求稳请继续用 v0.22.1。

### 修复

#### 一、展示标签只留一份：回执统一走 `STATUS_LABEL`（v0.22.9 核验 · 取舍 5）

旧实现里 `core/workflow.py::_fmt_results` 自带一份 `_tag` 标签表，与
`core/command_result.py::STATUS_LABEL` **并存**：同一份判定在回执里和别处
显示成不同字样，主人对不上号。

改后：回执直接取 `STATUS_LABEL`，兜底也写成「结果未知」——防御性兜底
绝不允许退回能被读成成功的措辞。用户可见变化：

| 状态 | 旧回执 | 新回执 |
| --- | --- | --- |
| `success` | `[OK]` | `[成功]` |
| `failed` | `[FAIL]` | `[失败]` |
| `unknown` / 缺 `status` | `[未知]` | `[结果未知]` |
| `dispatched_unconfirmed` | `[已发送·未确认]` | `[已发送·边界未确认]` |
| `inferred_success` / `syntax_error` / `skipped` | 不变 | 不变 |

#### 二、记账改 fail-closed：显式等长校验（v0.22.9 核验 · 取舍 6）

`_newly_confirmed()` 旧实现靠 `zip(commands, exec_reports)` 隐式配对 ——
长度不一致时 `zip` **静默截断**，多出来的命令既没记账、也没人发现。

改后：

```python
if len(commands) != len(exec_reports):
    return set(), f"命令与执行报告数量不一致：{len(commands)} 条命令 / {len(exec_reports)} 份报告"
```

结构对不上 → **不记账、不猜、停手问人**：新增 `_contract_halt_text()`
明确写出「无法判断哪几条真正发出去过」「不会自动重试」「重复副作用或漏发」，
并请主人先在游戏内确认实际结果。两处调用点（实现器循环 / 纠错循环）都处理。

#### 三、记账判据与熔断判据同源（皮莉卡自曝，GPT 未点名）

旧实现按 `r.get("ok")` 记账 —— 这是**第二套成功口径**：报告只要漏填 `status`
又带 `ok=True`（正是 v0.22.9 刚在熔断判据里堵掉的坏形状），就会被记成
「已生效」→ 下一轮同一条命令被当成重复而**跳过** → 主人要的东西少执行一次。

改后只认 `_status_of(r) == "success"`：**记账和熔断用同一双眼睛看**。
对 `_exec_commands()` 的正常输出零行为变化（它保证 `ok=True` ⟺ `status=success`），
只是把结构异常的报告从「记账」挪到「不记账」。

### 测试

- 新增 `tests/test_v02210_label_and_contract.py`：标签单一真相（七态覆盖）、
  等长校验、判据同源、记账→拦截闭环、静态契约、旧口径回归。
- 更新两处被本版改动的静态契约：`test_v0227_result_hardening.py`
  （原锁定 `"inferred_success": "已执行·未确认"` 字面量）、
  `test_v0229_failclosed_and_no_duplicate.py`（原锁定 `[OK]` / `"success": "OK"`）。
- 一键复现：`run_v02210_all.py`（自动探测 AstrBot 解释器，无需手动指定）。

## [v0.22.9] - 2026-09-22（预发布）

> 一句话：补掉两条「**默认成功**」的坏默认值 —— 执行报告缺 `status` 时不再回推成成功（改为 fail-closed 熔断），同一任务内**已确认生效**的命令不再重发（实现器自评「没做完」也不会把同一批命令再发一遍）。

> 本版为**预发布（pre-release）**：仍未在 Vanilla / Paper / Fabric 真实服务端做矩阵测试。
> 求稳请继续用 v0.22.1。

### 修复

#### 一、`_status_of` 改 fail-closed：缺 `status` 不再默认成功（v0.22.8 核验 P1-a）

旧实现：

```python
return report.get("status") or ("success" if report.get("ok") else "failed")
```

只要有一条执行报告漏填 `status`、又恰好带 `ok=True`，它就被判成 `success` ——
于是绕过 `_halt_on_uncertain` 的熔断，工作流继续往下跑。这与 v0.22.5 假成功事故
是**同一种坏默认值**：结构异常时默认成功。

现改为只认已知状态，其余一律 `unknown`：

```python
status = report.get("status")
return status if status in KNOWN_STATUSES else "unknown"
```

- 新增 `core/command_result.py::KNOWN_STATUSES`（= 状态标签表的键）作为**唯一**已知状态集合：
  缺字段 / 拼写错误 / 将来新增状态忘了登记，一律按「结果不确定」处理 → 熔断停手。
- **同源收口**：`_fmt_results` 里原本还躺着一份一模一样的回推逻辑
  （`r.get("status") or ("success" if r.get("ok") else "failed")`），现已改为调用同一个
  `_status_of` —— 只改一处留一处，正是「两套真相」的老病。
- 展示层标签表（`_fmt_results` 的 `_tag`）保留原样：它只决定措辞，不参与判定。

#### 二、同一任务内不重发「已确认生效」的命令（v0.22.8 核验 P1-b）

原逻辑：

```python
all_ok = all(r["ok"] for r in exec_reports)
if all_ok and out.get("success"):
    return ...
```

命令**全部成功**、但实现器自评 `success=false` 时，工作流会进入下一轮；
而实现器往往重新生成**同一批命令** → 原实现照发 → 重复副作用（例如再发一份物品）。

现改为「跨轮记账 + 执行前守门」：

- 本任务内**已确认成功**（`status == success`）的命令记入 `confirmed` 集合（实现器循环与纠错循环共用）；
- 每轮执行前把本轮命令拆成「还没发过的」与「已生效的」：
  - 部分重复 → 只发差额，重复条目跳过并写日志；
  - **整批都是重复 → 停手问人**：回执逐条列出「已生效·未重发」并给出裁决指引，
    既不自动重试，也不悄悄跳过；
- 进入下一轮时，把「已确认生效的命令」作为**既成事实**注入实现器上下文，
  明确要求不要重复生成；
- 命令比对只折叠空白与前导斜杠、**保留大小写**：宁可把两条判成不同，也不误拦语义不同的命令。

| 场景 | 旧行为 | v0.22.9 |
| --- | --- | --- |
| 全成功 + 自评未完成 → 下一轮重发同一批 | **重发（重复副作用）** | 停手，回执列出「已生效·未重发」 |
| 全成功 + 自评未完成 → 下一轮只给新命令 | 照发 | 不变（只发差额，多轮迭代保留） |
| 确定没生效（`failed` / `syntax_error`） | 纠错重试 | 不变（不得扩大熔断范围） |
| 结果不确定（`unknown` / `inferred_success` / `dispatched_unconfirmed`） | 熔断 | 不变（v0.22.8 口径） |

### 测试

- 新增 `tests/test_v0229_failclosed_and_no_duplicate.py`：53 项断言
  （六种异常状态输入 fail-closed、回执与判据同源、静态契约、命令规范化、
  重发拦截、混批只发差额、正常迭代不受影响、既成事实注入、v0.22.8 熔断口径未破）。
- 全量回归：`tests/test_*.py` 25 个文件全绿；`tests/ui_*.py` 真浏览器链路全绿。

## [v0.22.8] - 2026-09-22（预发布）

> 一句话：把「**结果不确定就不许自动重试**」收口成唯一实现 —— 模组命令的「已执行·未确认」不再让工作流重跑一遍（此前会把同一条命令再下发一次），并用真浏览器把「填写 `1.20.1` → 保存 → `item_syntax` 切到 `legacy_nbt`」这条链路钉死。

> 本版为**预发布（pre-release）**：仍未在 Vanilla / Paper / Fabric 真实服务端做矩阵测试。
> 求稳请继续用 v0.22.1。

### 修复

#### 一、`inferred_success` 不再触发自动重试（重复副作用保护）

v0.22.7 引入了 `inferred_success`（已执行·未确认）：`ok=False` / `accepted=True`。
其中 `accepted=True` 的用意**只是**「同批次后续命令可以继续发」，但复杂工作流的重试判据是
`all_ok = all(r["ok"] for r in exec_reports)` —— 于是这条链条成立：

```
模组命令返回「非空、无错误标记、也无成功证据」的输出
  → inferred_success（ok=False）
  → 本轮被判「没做完」→ 实现器重新生成命令 → 再发一次
  → 对未被 NON_IDEMPOTENT_COMMANDS 覆盖的模组命令 = 重复副作用
```

现改为**熔断**：出现不确定结果时立即停手并如实回执 —— 既不进下一轮实现，
也不交给纠错 Agent「换个写法重发」。

| 本轮结果 | 旧行为 | v0.22.8 |
| --- | --- | --- |
| `inferred_success`（模组命令非空输出） | 进下一轮 → **重发命令** | **停手**，回执「已下发但未确认」 |
| `dispatched_unconfirmed`（响应边界未确认） | 进下一轮 → **重发** | **停手**，回执「未确认」 |
| `unknown`（非幂等空响应 / 超时） | 停手 | 不变（措辞仍是「结果未知」） |
| `syntax_error` / `failed`（确定没生效） | 纠错重试 | **不变**（重发安全，不得扩大熔断范围） |

- 判据只有一处实现：`core/workflow.py::_halt_on_uncertain`（实现器循环与纠错循环共用），
  旧的「两处各写一段 unknown 熔断」已删除；新增模块级 `UNCERTAIN_STATUSES` 作为唯一定义。
- `CommandResult.accepted` 的文档写明 **「不构成自动重试的依据」**，
  模块文档同步点明 —— 防止后来者再把它当重试开关（这正是 P1 的成因）。

#### 二、熔断回执不再因缺字段而崩

`_fmt_results` 用 `r['output']` 硬取键，报告缺 `output` 时整条回执会抛 `KeyError`
—— 而回执是熔断时**唯一的出口**。现改用 `.get()`。

### 新增

- `tests/test_v0228_no_retry_on_uncertain.py`（33 项）：靶心复现（模组命令非空输出 →
  命令只下发 1 次、实现器只调 1 次、纠错 Agent 0 次）、三种不确定状态的熔断矩阵、
  混批（一条未确认即整体停手）、对照（语法错误仍可纠错重试）、静态契约（判据只有一处实现）。
- `tests/ui_version_override_check.py`（22 项，**真浏览器 Edge**）：验证版本控件完整链路 ——
  控件初值来自后端、只改草稿不生效、保存载荷带 `server_version_override=1.20.1`、
  保存后自动重刷能力状态、能力行显示 `legacy_nbt`（来源：主人手动声明）、
  清空后回到「无法确定服务端版本」。能力结论由 `core/version_caps.py` **现算**，不是手写假数据。

### 已销账（v0.22.7 的已知问题）

- ~~P1｜`inferred_success` 仍可能触发复杂工作流重试~~ → 本版修复（见上）。
- ~~WebUI 版本控件缺真实交互验证~~ → 本版补 `tests/ui_version_override_check.py`。

### 已知问题

> 本版仍未收口的问题，如实列出。

- **实现器自评未完成、但本轮命令全部成功**：此时 `all_ok=True` 而 `out["success"]=False`，
  工作流仍会进下一轮 → 实现器可能重新生成同一批命令再发一次。
  与本次修复**同源**，但改动会触及「多轮迭代」这一设计意图，故**未擅自扩大范围**，留待裁决。
- **未做真实服务端矩阵测试**：仅在本机 **1.20.1 · Forge 47.4.23** 实测，Vanilla / Paper / Fabric 未覆盖 ——
  这也是本版仍标 pre-release 的原因。

## [v0.22.7] - 2026-09-21（预发布）

> 一句话：把「**没发现错误就等于成功**」这条最后的侥幸通道彻底焊死 —— 非幂等命令的空响应与边界未确认一律判未知（熔断），RCON 异常按阶段区分「没发出去」和「可能发出去了」，并把服务端版本设置真正接到 WebUI 上。

> 本版为**预发布（pre-release）**：修复先落地观察一段时间，确认线上稳定再转正式版
> —— 尚未在 Vanilla / Paper / Fabric / Forge 真实服务端做矩阵测试。
> 求稳请继续用 v0.22.1。

### 修复

#### 一、非幂等命令的判定优先级（重复副作用保护）

`give` / `summon` / `effect` / `item` / `fill` / `function` 这类**跑两次就有两份后果**的命令，
在空响应与边界未确认两条路径上都必须熔断，不能被更宽松的通用分支截走：

| 场景 | 旧口径 | v0.22.7 |
| --- | --- | --- |
| 非幂等 + 空响应 + 边界已确认 | `success` | **`unknown`**（accepted=False） |
| 非幂等 + 空响应 + 边界未确认 | `dispatched_unconfirmed` | **`unknown`**（accepted=False） |
| 非幂等 + 非空输出 + 边界未确认 | `dispatched_unconfirmed` | **`unknown`**（accepted=False） |
| tellraw / say / title + 边界未确认 | `dispatched_unconfirmed` | 不变（消息类，允许） |
| time 等幂等命令 + 边界未确认 | `dispatched_unconfirmed` | 不变（重发无副作用） |

旧口径的问题不是「判错一个状态」，而是 `dispatched_unconfirmed` 带 `accepted=True` ——
工作流会**继续执行后续命令**，等于把 v0.22.4 刚建立的重复副作用保护拆掉了。

#### 二、`Operation aborted` 与错误词边界

- `Operation aborted` / `Refused` / `cancelled` / `rejected` / `denied` 此前**不含任何失败词**
  → 被判成成功。现已纳入失败短语表。
- 英文失败词改用**词边界**匹配：玩家名叫 `Error` 时
  `Gave 1 [Diamond] to Error` 不再被判 `failed`（实测复现过的误伤）。
- 命令级**锚定成功模式**（`^gave\b` / `^set the time\b` …）优先于全局失败词扫描 ——
  先看「这条命令自己的成功长什么样」，再谈失败。
- 连带修掉同源误伤：`_SYNTAX_MARKERS` 里的 `"expected "` 是 `"unexpected "` 的子串，
  于是 `An unexpected error occurred` 这类**运行期**错误被判成 `syntax_error`
  （`retryable=True`，等于谎称「重写就能安全重试」）。现改为词边界匹配。

#### 三、未知非空输出不再默认成功

模组命令（反馈文本不可枚举）的非空输出，在没有任何成功证据时降级为
**`inferred_success`（已执行·未确认）**，而不是 `success`：

- `ok=False`，回执与工作流汇总都明写「未确认」，不对外宣称成功；
- 非幂等命令连 `inferred_success` 都不给，直接 `unknown`；
- 想更保守可把 `INFERRED_SUCCESS_ENABLED` 置 `False`，一律落 `unknown`。

#### 四、RCON 异常分阶段

`RconError` 新增 `phase`（`connect` / `send` / `read` / `protocol` / `unknown`），
在**明确位置**构造而不是靠异常文本猜：

- `connect` → 命令确实没发出去 → `failed`；
- `send` / `read` / `protocol` → 命令**可能已经到达服务端** → `unknown`。

此前 `writer.write()` 写了半截、`drain()` 抛异常也被判失败，会诱导上层重发 `give` —— 重复发物品。

#### 五、execute 解析器全仓只留一份

`core/command_result.py` 不再自己手搓 `rfind(" run ")`（实测会把
`execute as @a run tellraw @a {"text":" run "}` 的命令名解析成 `"}"`），
改为复用 `core/java_commands.unwrap_command`。同一个解析问题只允许一处实现，
否则判定与权限迟早用上两套真相。

### 新增

- **WebUI 接通版本设置**（此前只有 schema 与后端，前端没有字段 —— 用户看不到也保存不了）：
  - 「连接」分组新增 `server_version_override`（手填版本）与 `item_syntax_override`（语法世代）；
  - 能力状态行直说结论：「Minecraft 1.20.1（来源：主人手动声明）· 物品语法：legacy_nbt（来源：手动指定）」；
  - 版本未知时明确提示：「⚠ 当前无法确定 Minecraft 服务端版本。含 NBT、附魔、物品组件的请求不会直接执行」；
  - 保存后立即刷新能力状态，**不需要重启插件**。
- `server/status` 的版本能力改到 RCON 连接**之前**计算 —— RCON 掉线时也能看见 Agent 到底按哪个版本构造命令。
- 服务器页消费 `version_hint`（此前后端只生成、前端没人显示，等于最重要的提示没到用户眼前）。
- `tests/test_v0227_result_hardening.py`：把上述每一条裁决钉成断言（空响应优先级矩阵、
  边界未确认矩阵、`Operation aborted`、词边界、锚定成功模式、`inferred_success` 语义、
  RCON 阶段、单一 execute 解析器、WebUI 接线）。

### 已知问题

> 下列问题由本版的独立核验（GPT 复核）指出，**本版未修**，留待下一版收口。

- **P1｜`inferred_success` 仍可能触发复杂工作流重试**（`core/command_result.py:655-659`、`core/command_result.py:314`、`core/workflow.py:388`、`core/workflow.py:441`）
  - 复杂工作流以 `all_ok = all(r["ok"] for r in exec_reports)` 判断本轮是否完成，而 `inferred_success` 的
    `ok=False`（`accepted=True` 只表示「不必熔断后续命令」）。于是「非空输出、无错误词、也无成功证据」的
    模组命令会让工作流判定本轮未完成 → 进入下一轮实现或纠错 → 同一条命令被再次执行。
  - 对**未被 `NON_IDEMPOTENT_COMMANDS` 覆盖的模组命令**，这仍可能造成重复副作用。
  - **本版规避手段（无需改代码）**：把 `INFERRED_SUCCESS_ENABLED` 置 `False`，此类输出一律落 `unknown` 并熔断。
  - 下一版方向：`inferred_success` 只允许对外显示「已执行·未确认」，不得自动重试；或在未知输出上直接判 `unknown`。
- **WebUI 版本控件缺真实交互验证**：`server_version_override` / `item_syntax_override` 目前只有静态契约测试与后端单测，
  尚未在真实浏览器验证「填写 `1.20.1` → 保存 → `item_syntax` 切到 `legacy_nbt`」的完整链路（计划补 Playwright 用例）。
- **未做真实服务端矩阵测试**：仅在本机 **1.20.1 · Forge 47.4.23** 实测，Vanilla / Paper / Fabric 未覆盖 ——
  这也是本版仍标 pre-release 的原因。

## [v0.22.6] - 2026-09-21（预发布）

> 一句话：修掉「命令明明失败却报成功」的**假成功**链路，并让**服务端版本**第一次真正进入 Agent 提示词 —— 此前 LLM 只能凭记忆猜版本，猜错就是把 1.21 的物品组件语法发给 1.20.1 服务端。

> 本版为**预发布（pre-release）**：修复先落地观察一段时间，确认线上稳定再转正式版
> —— 尚未在 Vanilla / Paper / Fabric / Forge 真实服务端做矩阵测试。
> 求稳请继续用 v0.22.1。

### 修复

#### 一、假成功链路（v0.22.5 线上事故的根因）

事故现场：要「附魔锋利5的下界合金剑」，分类器生成了

```
give HuangGuaKnn netherite_sword[enchantments={levels:{...}}] 1
```

发给 **1.20.1** 服务端，服务端回 `Expected whitespace to end one argument, but found trailing data` ——
而回执写「已执行 1/1 条命令」，游戏里什么都没有。三层同时失守：**分类器判错**、
**执行层只要不抛异常就算成功**、**回执丢弃明细**。

- **「什么算成功」收口成唯一实现** —— 新增 `core/command_result.py`，所有执行型入口共用；
  旧的 `workflow.py::_is_success_out()`（只看输出里有没有 `error` 字样）退役。
- **三个维度互不替代** —— `status`（这条命令结果是什么）／`response_received`（有没有收到属于**本命令**的响应）／
  `boundary_confirmed`（响应是否读到可靠结束边界）各自独立。
  `response_received=True` + `boundary_confirmed=False` 是**合法组合**：idle 降级模式下服务端确实答了这一条，只是不保证内容完整。
- **错误用黑名单、成功用「非空且无错误标记」** —— 严格白名单语义更干净，但会和既有**熔断**叠加出事：
  某条判 `unknown` 会让后续命令全部 `skipped`，而模组命令的反馈文本千奇百怪，
  「发枪 + 发弹药 + 反馈」这类多命令任务第一条误判就吞掉后面全部。
  显式成功标记（`Gave` 等）只用于把置信度从 `inferred` 抬到 `explicit`；
  日后要收紧为严格白名单，把 `STRICT_SUCCESS_MATCHING` 置 `True` 即可，调用方不用动。
- **幂等白名单（判据是幂等，不是静默）** —— 空响应不能一律当成功，也不能一律当失败：
  - **静默集合**（成功时本就不回文本）：`tellraw` / `say` / `title` / `actionbar` / `tm` / `teammsg` → 空响应即 `success`；
  - **幂等集合**（有回显，但重复执行无额外副作用）：`time` / `weather` / `gamerule` / `difficulty` / `gamemode` → 空响应也可当成功；
  - **非幂等集合**（重复执行会**叠加副作用**）：`give` / `summon` / `effect` / `xp` / `item` / `fill` / `clone` / `function` / `setblock` → 空响应**永远不能**当成功，判 `unknown`。
  - 若不先查白名单，`tellraw` 成功返回空串会被判 failed —— **连 `_send_feedback` 自身都走失败分支**（反馈发不出去的自噬）。
- **两类 `Unknown` 必须拆开**（只差几个词，语义相反）
  - `Unknown or incomplete command, see below for error` → **语法/参数写错**，命令本身存在 → `syntax_error`（可重试）；
  - `Unknown command. Type "/help" for help.` → **该命令在此服务端不存在**（模组没装 / 版本不支持）→ `failed`（重试徒劳）。
  - 归错就会对「模组命令不存在」白白重试一轮、再报一次同样的错，浪费一次 LLM 调用还污染纠错器记录。
- **判定顺序：成功判定必须早于 unknown 兜底** —— 若「无法判断」被实现成「没命中任何错误模式」，
  成功文本会先被吞成 `unknown`，成功分支永远不可达。现在的顺序是
  RCON 异常 → 空响应（按命令类型 + 边界） → 命令不存在 → 语法错误 → 其他明确失败 → **明确成功** → 兜底 unknown。
- **idle 模式下广播不得倒退成「失败」** —— idle 模式下 `boundary_confirmed` 恒为 `False`，
  照字面实现会让广播 / 喊话 / 全屏喊话从「成功」集体变成「结果未知」，属于功能倒退。
  现在对消息类命令单独一态 `dispatched_unconfirmed`，措辞为
  「已发送（响应边界未确认；如需保证完整性请把 `rcon_end_mode` 设回 `sentinel`）」—— **绝不显示「失败」**。
- **明细永不丢弃** —— `_run_simple` 的成功分支不再吞掉命令输出，失败 / 未知时逐条回执（含服务端原文），
  用户能看到到底是哪一条、错在哪。
- **铺到全部 11 个用户可见入口** —— `mc_execute_command` / `mc_broadcast` / `mc_give_item` /
  `mcs_say` / `mcs_title` / `mcs_kick` / `mcs_ban` / `mcs_unban` / `mc_kick` / `mc_ban` / `_send_feedback`。

#### 二、复杂任务路由守门（分类失误不再致命）

- **根因**：分类器 prompt 里「原版物品判 simple」与「带物品数据判 complex」两条规则**互相重叠**，
  实测把「附魔下界合金剑」判成了 simple —— 而 simple 路径**没有输出校验、没有纠错、没有到账核验**。
- **prompt 收紧**：明确 simple 只收**裸**原版物品（无附魔 / NBT / 组件 / 自定义名称 / 属性修饰符），
  规则冲突时**一律以 complex 为准**，并在规则里直写「把带数据的物品交给 simple 路径必然出错」。
- **确定性兜底 `looks_like_complex_task()`**：就算分类器再判错，只要请求或命令里出现
  附魔 / 属性 / 自定义名称 / NBT / 组件 / 满配 等特征，**强制执行**走有校验的复杂路径。
- **`[` 不再单独作特征** —— `[` 是选择器语法，`kill @e[type=item]`、`tp @p[tag=foo]`、`gamemode creative @a[team=red]`
  全会被误判进复杂流水线（白跑一整套 Agent、白烧 token）；而真正出事的 `netherite_sword[enchantments={…}]` **本来就含 `{`**。
  去掉裸 `[`、只留 `{`，捕获能力不减、噪音大降。

#### 三、服务端版本上下文（Agent 第一次「看得见」版本）

- **根因**：`detect_server_version()` 一直存在，但**只喂给了 WebUI** —— Agent 侧完全拿不到，
  LLM 只能凭记忆猜语法世代。猜错就是把 1.21 的组件语法发给 1.20.1 服务端（＝上面那场事故）。
- **新增 `core/version_caps.py`**，算出并缓存「语法世代」与「附魔 ID 口径」，在**分类器之前**注入 Agent 提示词：
  - 语法世代以 **1.20.5 为分水岭**（`{…}` NBT → `[…]` 组件），并处理快照 / 预发布号；
  - 附魔 ID 版本化（如 1.21+ 的 `sweeping_edge` vs 1.20 的 `sweeping`）；
  - **不猜**：版本探测不到（异地 RCON 模式必然失败）就**如实降级**并拒绝附魔 / NBT / 组件类请求，而不是默认成某个版本硬编。
- **优先级：手动声明 > 文件探测 > 未知** —— 新增配置 `server_version_override`（手动声明服务端版本）
  与 `item_syntax_override`（手动指定物品语法世代），异地模式下这是唯一出口。
- **WebUI 与 Agent 看同一份数据** —— 服务端状态接口新增 `version_caps`，手动声明生效时把显示版本标注为
  「（手动声明）」；否则会出现「UI 显示 1.21、Agent 按 1.20.1 构造」这种最难查的错位。
  版本未知时页面直说：「附魔 / NBT / 物品组件类请求会被拒绝」。

### 新增

- 配置项 `server_version_override`：手动声明服务端版本（异地 RCON 模式必备）。
- 配置项 `item_syntax_override`：手动指定物品语法世代（覆盖探测结果）。
- `core/command_result.py`：统一命令结果判定（三态 + 幂等白名单 + 两类 Unknown 拆分）。
- `core/version_caps.py`：版本能力表（语法世代 / 附魔 ID / 未知降级）。
- 命令结果新状态 `dispatched_unconfirmed`：「已发送·边界未确认」，与「失败」「结果未知」三者互不冒充。

### 测试

- `tests/test_command_result.py`（新增）—— 三态结果判定 / 命令不存在与语法错误拆分 / 复杂 NBT 路由守门。
  对照测试含：`tellraw @a {"text":"hi"}` + 空输出 → `success`（**不是** failed）；
  `give Steve diamond 1` + 空输出 → `unknown`；删掉幂等白名单 → 广播用例必须变红。
- `tests/test_simple_workflow_result.py`（新增）—— simple 与复杂两条路径均不再假成功 / 明细永不丢弃 / 分类 prompt 不再自相矛盾。
- `tests/test_version_capabilities.py`（新增，42 项）—— 版本能力表能算出正确的语法世代与附魔 ID，版本未知时如实降级且留有出口。
- `tests/test_complex_routing.py`（新增，30 项，含端到端转轨）—— 分类失误不再致命：带数据的请求必被拦进有校验的复杂路径，且版本上下文先于分类器注入。
- 全仓 **31 支断言脚本**（22 个 `test_*.py` + 9 个 `ui_*.py`）使用 AstrBot 自带解释器执行，退出码全为 0。

## [v0.22.5] - 2026-09-21（预发布）

> 一句话：修掉「设置页一保存 RCON 配置就**整台插件再也不回话**」的自锁死 —— `reset_rcon()` 持锁后又去抢同一把非重入锁；顺带堵住退役实例的**连接泄漏**，并把运行态展示改成**三状态互不替代**的诚实口径。

> 本版为**预发布（pre-release）**：修复先落地观察一段时间，确认线上稳定再转正式版
> —— 尚未在 Vanilla / Paper / Fabric / Forge 真实服务端做矩阵测试。
> 求稳请继续用 v0.22.1。

### 修复

- **`reset_rcon()` 自死锁（P0，能整台插件哑掉）**
  - v0.22.4 的 `reset_rcon()` 在 `async with self._rcon_lock:` 里又 `await self._get_rcon()`，而 `_get_rcon()` 要抢**同一把不可重入**的 `asyncio.Lock` —— 该协程永久挂起，锁永不释放：此后所有 RCON 调用一起排队等死，且**没有任何异常、没有任何日志**，表现就是「插件突然装死」。
  - 触发条件极窄但极常见：设置页改任意 RCON 项（或点「重建 RCON 连接」）保存一次即中。
  - 现在抽出同步纯构造器 `_build_rcon()`，两处共用：`_get_rcon()` 只在锁内做「取或建」，`reset_rcon()` 在**同一把锁内**完成「摘旧 → 建新 → 返回」，既不重入也不再留空窗。
- **退役实例的连接泄漏（P2 收尾）**
  - 旧写法 `if not old._connected: await old.close()` 恰好判反：**空闲但连着**的旧实例不会被关（socket + StreamWriter 被无声遗弃），只有「本来就没连」的才走关闭分支。
  - 新增 `AsyncRcon.in_flight`：`reset` 只在**没有在途命令**时才亲自收尾；若旧实例正握着一命令，则让它跑完、由 `command()` 的 `finally` 自行回收（在途调用绝不因换连接被判成「结果未知」）。

### 修复（v0.22.5 复审整改）

- **边界状态必须反映「最近一次」（P2）** —— 旧实现只有「哨兵成功」写 `True`、「idle 返回」写 `False`，**超时 / 零响应 / 协议异常路径什么都不写**：上一次成功留下的 `True` 会被原样沿用，页面于是显示「✓ 边界可靠」，而最近一次执行其实「结果未知」，字段与事实相反。
  - 现在每条命令**事务开始时先把 `last_boundary_confirmed` 置为 `False`**，只有哨兵按序到齐才写 `True`；字段初始值改为 `None`（还没执行过命令 = 没有结果可谈，不得默认成 `True` 冒充可靠）。
- **运行态三状态互不替代（P2，前端）** —— 旧前端只判 `degraded`：主人**亲手**把 `rcon_end_mode` 配成 idle 时 `degraded=false`，页面照样显示「✓ 边界可靠」，等于把「没有自动降级」当成「响应可靠」。
  - 现在三条各自独立显示：① `end_mode === "idle"` → 「⚠ 静默窗口模式」+ 明写「**响应完整性未保证**」并标出静默窗口秒数；② `degraded === true` → 额外「⚠ 已自动降级」并同时列出「当前运行 / 配置」两种方式；③ `boundary_confirmed === false` → 「⚠ 最近一次响应可能不完整」；`null` → 「尚未执行过命令，暂无边界确认结果」。
  - 顺带把累计未确认次数（`idle_unconfirmed`）与连续探测失败次数（`probe_misses`）显示出来，重建按钮的恢复路径就写在提示里。

### 测试

- `tests/test_v0225_idle_reset.py`（新增，35 项全绿）
  - P1：idle 降级模式下「读到半截就停」必须废弃脏连接 —— 且**下一条命令要干净返回**（不吃上一条残留的 `LAST`）；含正常单包对照组、零响应仍须抛「结果未知」、哨兵乱序保持 v0.22.3 行为。
  - P2：退役实例拒绝重连、**在途命令不被掐断且跑完后自动关连接**、`reset` 显式关闭空闲旧连接、连点 5 次并发重建不炸不漏不自锁、换下的实例不得再执行命令。
  - 复审补充（11 项）：三态起点 `None`、哨兵到齐才 `True`、**「上一次成功 → 这一次超时/协议异常」不得沿用 `True`**（零响应 / 哨兵乱序两条路径各一条）。
- `tests/ui_fp_notice_check.py`：新增第 [8] 组「运行态三状态互不替代」12 项 —— 显式配置 idle 时**不得**出现「✓ 边界可靠」、自动降级要显示当前/配置两种方式、健康态不得误报「响应可能不完整」、未执行过命令要说「尚未执行过命令」、点重建后回到配置方式且边界状态不沿用旧的 `False`。
- `tests/_paths.py`：`require_app()` 现在会拦住「在事件循环里 import astrbot」—— 那种调用会**假死**（进程活着、零输出），此前排查成本极高，现在直接报清楚错误。该 import 必须放在模块加载阶段。
- `tests/test_remote_rcon_mode.py`：插件替身补 `_rcon_lock`（与新实现对齐）。

### 核验说明

- 本次改动经**变异对照**确认非空跑：把「命令事务开始置 `False`」那一行去掉后，`test_v0225_idle_reset.py` 用例 ⑤ 立刻变红（字段停留在 `True`）；把前端 `renderRconRuntime` 换回旧逻辑后，`ui_fp_notice_check.py` 第 [8] 组立刻 7 项红。
  （两个对照脚本均为临时文件，跑完已删，不进仓库。）
- 全仓 27 支断言脚本（18 个 `test_*.py` + 9 个 `ui_*.py`）使用 AstrBot 自带解释器执行，退出码全为 0。

## [v0.22.4] - 2026-09-21

> 一句话：修掉 v0.22.3 哨兵机制的**顺序漏洞** —— 哨兵抢在命令响应之前到达时会被静默当成「合法空响应」；并把「非幂等命令 + 空响应」这条重复副作用风险一并收口。

### 修复

- **哨兵乱序仍会被静默当成成功**（P1，v0.22.3 核验发现）
  - 旧实现收到哨兵就 `break` 返回：若服务端**先回哨兵、后回命令响应**（不保证按序回应），本次调用会返回空字符串、`connected` 仍为 True，而迟到的命令响应还留在连接里等下一次调用去踩 —— 既谎报成功，也没有兑现「晚到包会触发请求 ID 不匹配」的承诺。
  - 现在把顺序当作硬要求：**必须先收到本次命令的响应包，哨兵才算结束边界**（payload 为空也算「收到了」）。哨兵抢先到达时立即废弃连接并抛 `RconPartialResponseError`（**结果未知**），绝不当成「合法空响应」。
  - 「命令响应 → 哨兵」按序到达时的空 payload 仍照旧是**合法空响应**（返回空字符串），语义不变。
- **非幂等命令的空响应不再当失败去重试**（同类重复副作用风险）
  - `give` / `summon` / `effect` / `item` / `xp` / `experience` / `fill` / `setblock` / `clone` / `function` / `random` / `spreadplayers` 等**重复执行会产生额外副作用**的命令，若只拿到「合法空响应」就被判成失败，纠错 Agent 会重试 → 重复发物品。
  - 现在这类命令只收到空响应即标为**结果未知**（`status=unknown` + 立即熔断、后续命令不再发送），提示「已收到合法空响应，但效果无法确认」；解析不出结构的复杂 `execute`、模组 / 未知命令同样按最保守处理。
  - 普通命令（如 `say`）的空响应仍按既有口径判失败 —— 不扩大熔断范围（新增对照组用例守着这条边界）。
- **自动降级「看得见、可恢复」**
  - 只有连续多次**无法确认响应边界**（服务端不回应哨兵 / 哨兵顺序异常）才降级；服务器整体失联**不再计入**（那与哨兵机制无关）。
  - 概览接口新增 `rcon_runtime`（当前运行方式 / 配置方式 / 是否已降级 / 连续失败次数）；设置页常驻显示运行态徽标，并新增「↻ 重建 RCON 连接」按钮（`POST /rcon/reset`）一键回到配置方式。
  - 降级日志写明「多包响应的结束判断不再绝对可靠、长响应可能被截断」与恢复途径。

### 文档

- `docs/configure.md`：补充 `rcon_end_mode` / `rcon_probe_command` / `rcon_idle_probe` 三项说明，并同步配置总项数（86 项）。
- `docs/faq.md`：新增两条 —— 「设置页提示 RCON 已自动降级是什么意思」，以及「非管理员为什么有些 `execute` 写法会被拒绝」（含 `if items` / `if function` 等边界判不出的条件一律拒绝的 fail-closed 口径与管理员例外）。

### 测试

- 新增乱序用例（真·乱序服务端：命令响应延后 50ms、哨兵立刻回）：必须抛结果未知并废弃连接；连续 3 次后自动降级且有日志；降级后恢复正常返回。
- `tests/test_rcon_protocol.py`：`swallow`（只回哨兵不回命令）从「合法空响应」改判为**顺序异常 / 结果未知**并补断言；`empty`（空响应包 + 哨兵按序到达）仍判合法空响应。
- 新增空响应用例（含对照组）：非幂等命令空响应 → unknown 且不发送后续命令；普通命令空响应 → 仍按既有口径。
- 全仓库 17 个测试脚本 + `compileall` + 设置页 DOM 自检全绿。

## [v0.22.3] - 2026-09-21

> 一句话：把 v0.22.2 遗留的三处最危险全部收口 —— RCON 改用**结束哨兵**判定响应已收完（不再靠猜）、工作流拿到「结果未知」立刻熔断（绝不再发第二遍）、execute 权限解析改成**解析不出来就拒绝**（fail-closed）。

### 修复

- **RCON 分包仍可能被截断**（P1）
  - 旧版用 150ms 静默窗口猜「响应读完了」：首包后 220ms 才到次包时只拿到 `FIRST`、剩下的包残留在连接里串进**下一条**命令；次包只先到半截时还会留下 `connected=True` 但读取状态已错位的连接。
  - 新增**结束哨兵（sentinel，默认）**：发包后紧跟一条无害探测命令（默认空命令）。服务端按序处理命令、TCP 按序送达，因此「收到探测命令的第一帧」等价于「上一条命令的响应已全部到达」—— 这是协议层可证的结束边界，与网络延迟 / 分包间隔 / 半包停顿都无关。
  - 新增 `RconPartialResponseError`（继承 `RconTimeoutError`，语义同样是**结果未知**，附带 `partial` 已收到的内容）：拿到部分输出却没等到结束边界时抛出，**绝不静默返回半条响应**，并废弃连接。
  - 「协议异常一律废弃连接」补齐：包长度非法、包尾缺少两个 NUL 字节、响应包数超过 `MAX_PACKETS`、读取中断、半包被取消 —— 任何「流已错位」的情形都关连接，不留 `connected == True` 的错位连接。
  - 服务端不回应探测命令时：连续 3 次自动降级为 `idle` 静默窗口并写日志（`PROBE_AUTO_DEGRADE_AFTER`），也可在设置页把 `rcon_end_mode` 显式设为 `idle`。新增配置项 `rcon_end_mode` / `rcon_probe_command` / `rcon_idle_probe`（默认 `sentinel` / 空命令 / 0.5 秒）。
- **工作流会重复执行「结果未知」的命令**（P1）
  - `_exec_commands()` 结果升级为**三态**：`success` / `failed` / `unknown`（另加 `skipped`），保留 `ok`、`unknown` 旧字段以兼容既有调用方。
  - 一旦某条命令结果未知：**立刻停止后续命令**（不再发送，标 `skipped` 并在明细里写明原因），实现器循环与纠错循环都**立即熔断**，返回「工作流已暂停：有命令执行结果未知……请先用 状态 / 在线列表 确认后再手动发起」—— 不再自动重试、不再交给纠错 Agent 重发。`give` / `summon` / `effect` / `item` 这类非幂等命令不会再被发第二遍。
  - 明确失败（例如服务器返回错误文本）仍按既有流程重试，不会被误熔断成「结果未知」。
- **execute 权限解析仍可绕过**（P1）
  - 旧版「找第一个 `run`」：`execute as run run stop` 里的首个 `run` 其实是**目标实体名**，真正的 `stop` 被漏判放行；超过 16 层上限后还会把剩余文本当普通命令继续判定（fail-open）。
  - 新版按 **execute 语法**逐个消费子命令（`as / at / positioned / rotated / facing / align / anchored / in / if / unless / store / run`），只有出现在合法分界位置的 `run` 才算执行边界；并按 Minecraft 词法切词（引号、方括号、花括号内部不切），参数里带空格或 JSON 的写法不会被误判。
  - **fail-closed**：嵌套超过 `MAX_EXECUTE_DEPTH`（8 层）、子命令或条件边界判不出来、`run` 之后没有命令 —— 一律**拒绝**，绝不「解析失败就放行」；`is_danger_command()` 在解析失败时按危险处理。
  - 命名空间只对**命令名** token 去前缀（`minecraft:gamemode` → `gamemode`），参数里的 `mod:item`、`ns:function`、`minecraft:overworld` 保持原样，不再被改写含义。
  - 顺带修正 `if block <pos> <block>` 这类位置参数的 token 数：`execute if block ~ ~-1 ~ stone run say hi`、`execute positioned as @a ~ ~ ~ run ...` 等正常写法照常放行。
- **不使用标准库 logging**（上架规范）
  - `core/rcon.py` 的自动降级告警改为**由插件注入日志器**（`main.py` 传入 AstrBot 的 logger）：协议层保持零依赖、可独立单测，同时满足「插件代码里不得出现标准库 logging」的审核要求。

### 测试

- 新增 `tests/test_v0223_hardening.py`（RCON 端 20 项 / 权限端 20 项 / 工作流端 11 项）：
  - RCON：220ms 延迟分包、1s 延迟分包、次包先到半截、只收到 3 字节包头、非法长度、缺包尾、包数超限、合法空响应、哨兵无响应自动降级、显式 idle 模式、任务取消废弃连接。
  - 工作流：未知即熔断、后续命令一条不发、纠错循环不重发、明确失败不误熔断、三态明细标记。
  - 权限：`execute as run run stop`、17 层包装、无法判定的条件、未知子命令全部拒绝；正常 `execute` 写法与 8 层以内包装照常放行。
- `tests/test_rcon_protocol.py` 的「无响应」用例改为真·卡死（连哨兵也不回），并补「只回哨兵不回命令 = 合法空响应」的语义用例。
- 全仓库 17 个测试脚本与 `compileall` 全绿。

## [v0.22.2] - 2026-09-21（预发布）

> 一句话：把 RCON 这条链路上四个「平时看不见、一并发就咬人」的坑一次修干净 —— 共享连接不再互踩、长回复不再截断串包、超时不再谎报成功、黑名单再也绕不过去。

> 本版为**预发布（pre-release）**：先让修复落地观察一段时间，确认线上稳定再发正式版。
> 想稳妥运行请继续用 v0.22.1。

### 修复

- **共享 RCON 连接的并发互踩**（同一条连接被聊天工具 / WebUI / 工作流同时使用）
  - `AsyncRcon` 新增 `asyncio.Lock`，`command()` 的**整条事务**（必要时建连认证 → 发包 → 读完整响应）都在同一把锁内完成 —— 并发调用只会排队，不会互相读走对方的响应包。
  - 旧版并发时两条命令抢同一个 socket，后到者会读到前一条的包体、`readexactly` 直接炸掉（半包），是「偶发报错」的真正来源。
  - `close()` 同样进锁，避免关连接与进行中的命令抢 socket；`connect()` 改为幂等（已连着就直接返回，不重复握手）。
- **长回复被截断、遗留包串进下一条命令**
  - 旧版收到第一个匹配 id 的包后只「短等 0.15s 补一包」就收工：三包响应只能拿到两包，剩下的包残留在连接里、混进**下一条**命令的输出。
  - 新版按「已收到包就短探测分片、一个包都没收到就用剩余总预算等」的循环收齐（`IDLE_PROBE` 探测分片、`MAX_PACKETS` 防异常服务端无限刷包），多包响应完整拼接。
  - **严格校验请求 id**：收到不属于本次请求的包 → 判定连接上残留着上一轮数据，**废弃连接**并明确报错，绝不把残留内容混进本次输出。半包被取消（`CancelledError`）时连接状态不确定，同样废弃。
- **超时被当成「已执行」**（会给玩家错误的安全感）
  - 新增 `RconTimeoutError`（继承 `RconError`，带 `partial` 已收到的部分输出），语义是**结果未知**：命令可能已执行、也可能没有。
  - 超时后连接被废弃，不复用状态不确定的连接（下一条命令会自动重连，不会永久失联）。
  - `main.py` 的四个入口（执行指令 / 发放物品 / 踢人 / 封禁）全部单独捕获，明确回报「结果未知……为免重复副作用，本次不会自动重发，请先用 状态 / 在线列表 等查询命令确认」，不再对外宣称成功。
  - `core/workflow.py` 的简单路径与批处理路径同样单独捕获，批处理结果额外标 `unknown: True`，与「明确失败」区分开。
  - **合法空响应 ≠ 没收到响应**：确收到响应包但内容为空，照常返回空字符串，文案改为「命令已执行（服务器未返回输出）」—— 两件事不再混为一谈。
- **黑名单策略的 `execute` 包装绕过**
  - 旧版危险命令判定只剥一层，`execute run stop`、`minecraft:gamemode spectator @a` 这类写法能把危险命令带过去。
  - 新增 `unwrap_command()`（剥 `execute … run` 包装，**支持多层嵌套**，16 层上限防死循环）、`strip_namespace()`（去 `minecraft:` 前缀）与 `canonical_text()`（剥包装 → 逐 token 去命名空间 → 压空白 → 转小写）；`match_danger_rule()` 按「命令名 + 参数**整词**」匹配，因此 `gamemode spectator` 这类带参数的规则也能拦住包装写法。
  - 结果：`gamemode spectator @a`、`minecraft:gamemode spectator @a`、`execute as @a at @s run minecraft:gamemode spectator @s`、`execute run execute run stop` 判定完全一致；`gamemode creative`、`execute as @a run say hi` 等正常命令照旧放行。
  - `effective_level()` 同步改用解包后的命令名，**权限等级解析与危险命令判定走同一个口径**。

### 测试

- 新增 `tests/test_rcon_protocol.py`：本机起一个 loopback 假 RCON 服务（**不需要真实服务端**），把这次修的四类问题钉成 34 项断言 —— 并发两命令各拿各的且连接仍可用、三包响应完整收齐、两包拼接正确、多包之后下一条命令干净、残留包被拒且连接废弃、合法空响应与无响应语义分开、超时按预算返回且文案点明「结果未知」、超时废弃后下一条自动重连，以及 11 条包装绕过 + 6 条正常命令的权限回归。

## [v0.22.1] - 2026-09-18

> 一句话：把 v0.22.0 被市场审核驳回的两条意见修掉了 —— 日志器全部改走 `astrbot.api`，权限前置提醒不再改写系统提示词（改挂用户消息的额外内容块，保住提示词前缀缓存）。

> 审核原文两条：1)【必须整改】日志记录：`core/hot_reload.py`、`core/agent_llm.py` 用了标准库 `logging.getLogger("astrbot")`，规范要求日志器必须且只能是 `from astrbot.api import logger`；2)【建议优化】上下文注入：`main.py` 的权限前置提醒直接向 `req.system_prompt` 追加含请求者 ID 的动态内容，会影响缓存命中率。

### 修复

- **日志器统一到 `astrbot.api`**（上架规范「必须整改」项）
  - `core/hot_reload.py` 与 `core/agent_llm.py` 的 `import logging`、`logging.getLogger("astrbot")` 全部删除，改为 `from astrbot.api import logger`；`hot_reload` 的 6 处日志调用点直接使用该 logger（保留 `plugin_tag` 注入）。
  - 顺手清掉 `core/agent_llm.py` 里那一行**死代码**：`self._logger = ...` 原先落在 `_fallback_cfg()` 的 `return` 之后，永远不会执行 —— 也就是说**未传 `logger` 构造 `AgentLLM` 时 `self._logger` 根本不存在**，一旦要打警告就会 `AttributeError`。现在赋值挪进 `__init__`，插件传入的 logger 仍优先。
- **权限前置提醒不再改写系统提示词**（上架规范「建议优化」项）
  - `main.py` 新增统一注入出口 `_append_user_hint()`：四处注入点全部改为把提示块追加到 `req.extra_user_content_parts`（格式 `{"type": "text", "text": ...}`，与 AstrBot 自身的系统提醒同一注入点，拼在同一轮用户消息的尾部）。
  - 语义不变：模型在同一轮照样读得到「哪些工具会被拒、有哪些替代方式」；变的是**系统提示词一个字都不动**，前缀缓存不再因为请求者 ID 变化而失效。
  - 老版本 AstrBot 若没有该字段，**宁可不提醒**（权限闸门本身仍然拦得住），也不回退去写 `system_prompt`。
  - 配置项 `permission_hint_injection` 的说明同步改为「注入本轮对话」，`_conf_schema.json`、`docs/configure.md`、插件 WebUI 设置页三处口径对齐。

### 测试

- 新增 `tests/test_review_compliance.py`：把这次的两条审核意见钉成自动化护栏 —— 发布包内的源码（`main.py` / `core/`）不得出现任何标准库 logging 用法、不得给 `.system_prompt` 赋值；并做运行期验证（`hot_reload` 的模块级 logger 就是 `astrbot.api.logger`、`AgentLLM` 不传 logger 时 `_logger` 仍可用）。
- 更新 `tests/test_tool_guard.py`、`tests/test_remote_rcon_mode.py`：提示改从 `extra_user_content_parts` 读取，并新增「`system_prompt` 一字未动」断言（含老版本无该字段时也不回退）。

## [v0.22.0] - 2026-09-18

> 一句话：知识库检索一口气连升三档 —— 「换种说法也能查到」的语义通道、召回之后的精排、以及手动指定嵌入 / 重排模型；顺带把设置页那一排卡片的高度、广播页的叫法都收拾齐了。

> 本节汇总了 v0.21.41 ~ v0.21.45 五个未单独发布的预发布改动，明细一条没删。

### 新增

#### 语义增强检索（原 v0.21.41）
- **语义增强检索**（配置项 `knowledge_semantic_search`，设置页「词典与知识库」卡片，
  **默认关闭**）：知识库在 BM25 词法通道之外，再走一条「嵌入向量」语义通道，两条通道
  各排各的名次、再用 **RRF（倒数排名融合）** 合并。开启后提问可以「换种说法」——
  问「后台连不上了」也能捞到库里那条「RCON 连接失败排查」。
  - **只建议大型服务器启用**：每次检索多一次嵌入调用（约 300~500ms 延迟）；建库时每条
    约一次嵌入调用（后台分批补算、只算新增 / 改动过的条目、结果落盘缓存不重复烧）。
    条目少的小库用 BM25 已经够准，这份开销不值。
  - **前提**：需先在 AstrBot「服务提供商」添加一个 Embedding 模型（推荐文本嵌入模型）。
    没配置时开关开了也不报错，安静退化为纯 BM25。
  - 本机实测（438 条语料 / 24 组带标准答案查询 / 槽位 K=6）：

    | 检索方式 | Hit@1 | Hit@6 | MRR | 无关查询误召回 |
    | --- | --- | --- | --- | --- |
    | 纯 BM25 | 75.0% | 79.2% | 0.771 | 0 |
    | ＋语义（无门槛） | 83.3% | 91.7% | 0.868 | 18 |
    | **＋语义（余弦门槛 0.50）** | **87.5%** | **100.0%** | **0.931** | **0** |

    拆分看：字面型查询 Hit@1 91.7% → 100%；口语改写型查询 Hit@1 58.3% → 75.0%、
    Hit@6 58.3% → 100%。换多模态嵌入模型时语义通道被压弱但**仍不低于纯 BM25**，不会把命中挤掉。
  - 新增余弦相似度门槛 `SEMANTIC_MIN_SIM = 0.50`：RRF 只看名次、不看分数，无关提问照样会被
    塞进候选池并加分（实测 3 条无关查询的返回条数从 0 涨到 18）。加一道绝对门槛后误召回归零、
    指标反而更高。相关查询 top1 余弦最低 0.505、无关查询最高 0.460，0.50 正好卡在两者之间。

#### 重排序精排（原 v0.21.42）
- **重排序精排**（配置项 `knowledge_rerank`，设置页「词典与知识库」卡片，**默认关闭**）：
  检索升级为「召回 → 精排」两段式 —— 先用 BM25（可叠加语义通道）召回 `RERANK_POOL = 20`
  条候选池，再交给 **Rerank 模型（Cross-Encoder）** 逐条打分重排，最后取前 `limit` 条。
  召回看的是「词 / 向量像不像」，精排看的是「查询与条目的真实相关性」，能把召回到了但
  排不前的正确条目提上来。
  - **与语义通道相互独立**：没有 Embedding 模型也能单独开（只对 BM25 召回池精排）；
    两者同开就是「BM25 + 向量 RRF 召回 → rerank 精排」的完整三段式。
  - **前提**：需先在 AstrBot「服务提供商」添加一个 Rerank 模型（如 `vllm_rerank` /
    `Qwen3-Reranker-8B`）。取已加载的第一个 Rerank Provider，不按 id 硬编码。
    没配置时开关开了也不报错，安静退化为召回顺序。
  - **代价**：每次检索多一次重排 API 往返。小库（几十条）BM25 已经够准，不值得开。
  - 失败即退化：rerank 调用异常时自动退回普通检索，精排永远不该让整次检索失败。

#### 手动指定嵌入 / 重排序模型（原 v0.21.43）
- **手动指定嵌入 / 重排序模型**（配置项 `knowledge_embed_provider_id`、
  `knowledge_rerank_provider_id`，设置页「词典与知识库」卡片，**默认留空 = 自动**）：
  - 留空 = 用 AstrBot 里加载的第一个（与之前的行为完全一致，老配置无需改动）。
  - 填了就用指定的那个 —— 两台嵌入模型（比如一个本地小模型、一个远程大模型）时可以按需挑。
  - 支持空间有限：WebUI 设置页两个下拉直接从 AstrBot **已加载的 Provider 实例**读取，
    「配置里有但没启用 / 加载失败」的模型不会出现在候选里 —— **下拉能选的一定可用**，
    不用手抄 provider ID。
  - 指定失效（模型被删除 / 停用 / 改名）时**安静回落到第一个**，保存设置时提示
    「已回落」并写明实际生效的是哪一个，不会让检索失效、也不会让人误以为指定的生效了。
  - 保存即生效（无需重载插件）：换 **嵌入模型** 会触发**后台整库重算向量**
    —— 不同模型的向量维度与语义空间都不通用，留着旧的会串味；换 **Rerank 模型**
    只换注入，无需重算。
  - 「当前生效」会在设置页直接标出来（`自动` 还是 `指定值 · 模型名`）。

### 修复

- **知识库当前生效模型永远显示为空**：`kb_model_status` 把「给前端下拉的 dict 列表」喂给了
  挑模型函数，而 dict 上没有 `provider_config` —— 导致当前生效模型显示为空、失效回落永不触发。
  改为统一走 Provider **实例列表**（`_provider_insts`），并由测试钉住（新增回归护栏）。（原 v0.21.43）
- **向量入库 O(n²) 复制**：`SemanticIndex.set_vectors()` 原本每加一行就 `vstack` 一次，
  5000 条要 21 秒。改为整批拼接后 0.14 秒（**提速约 150 倍**），批量建库不再卡顿。（原 v0.21.41）
- **换嵌入模型时拿旧向量硬算**：向量维度与缓存不符时明确放弃本次语义通道、退化为纯 BM25。
  （原 v0.21.41）
- **广播控制台的配色说明写反了方位**：原文写「颜色跟随**上方**「文本颜色」设置」，
  可颜色卡其实在它**下面**，已改为「下方」，并直接点明「各用同名那一格的颜色」。（原 v0.21.45）

### 变更

- **设置页排版：知识库卡的说明收进折叠区**（`details.kb-fold`，**默认收起**，点标题展开 / 收起）：（原 v0.21.44）
  - 原先这张卡靠 5 段长 hint 堆到约 1260px，同排的「LLM 工具开关」被 grid 拉伸后，
    底部空出 **814px** —— 看起来像卡片没写完。现在两卡内容高度 463px / 496px，空档 **50px**。
  - 7 个开关收成两列（词典 2 + 知识库工具 3 + 语义增强检索 + 重排序精排）；两个模型下拉并排成一行。
  - **「重排序精排」不再横跨两列**（曾写成 `grid-column:1/-1`，一个开关占两格、看着像没写完）——
    恢复单格，与左列开关同一竖直位置。
  - 「语义增强检索」副标题改为「加嵌入向量通道（仅推荐大型长期运行服务器开启）」：
    文案长了一档，在括号前主动断行，避免折成「……长期运行服务\n器开启」这种半截句；
    两行副标题会让同行两格不等高（虚线一高一低），因此 `.kb-card .sw-grid` 改用
    `align-items:stretch` —— 短的那格虚线照样落在行底，仍是一条直线。
  - 原理解释、实测数据、代价与前提**一字未删**，只是从卡片正文挪进折叠区
    （检索引擎 / 语义增强检索 / 重排序精排 / 模型回落 / 生效时机，共 4 段）。
  - 折叠区样式只作用于 `.kb-card`（`details.kb-fold`），全局 `details` / `summary` 规则未动。
- **广播控制台与「文本颜色」的叫法、顺序统一**（原 v0.21.45）：广播控制台的三个模式是
  「聊天栏 → 全屏标题 → 模拟任务输出」，下面「文本颜色」卡片却是「全屏喊话 → 普通喊话 → 任务输出」
  ——顺序反着、叫法也不一样。现在三格改成「聊天栏 / 全屏标题 / 模拟任务输出」，与上方 tab 一一对应；
  **设置页**那张「文本颜色」卡里的三段「单色 / 渐变锚点」也一并调成同一顺序、同一叫法。
  原生配置页与文档跟着统一叫法（`color_say` / `color_title` / `color_feedback` 三组颜色与渐变锚点），
  只改显示名，配置键与默认值**一个没动**（老配置照旧生效）。
  注意 `mcs 全屏喊话` 是**指令名**，与「全屏标题」这个模式名不是一回事，故保持原样未改。

### 工程

- 取模型路径收敛成三处共用：`_resolve_embed_fn` / `_resolve_rerank_fn` / `kb_model_status`
  都走 `_provider_insts()` + `_pick_provider()`，不再各自 `insts[0]` 硬取第一个。（原 v0.21.43）
- 召回逻辑从 `search()` 抽成 `_rank_candidates()`、模板补召抽成 `_template_fill()`，
  普通检索与精排检索**共用同一套召回与补召口径**，避免两条路径行为漂移。（原 v0.21.42）
- `asearch()` 分流：精排开启且注入了可用调用时走 `_search_with_rerank()`，否则维持原路径。
  精排开关热生效：保存设置即注入 / 撤销 rerank 调用，无需重载插件；切换预设、
  换服务端重建 KB 实例时同步带上开关状态与 rerank 调用。（原 v0.21.42）
- 设置页与配置页文案里的实测数值全部对齐到本次复测结果（不写估算值）。

### 测试

- 新增 `tests/test_kb_semantic_search.py`（14 组断言，纯离线桩向量、不烧 API）：
  融合确实生效、门槛上下边界（0.52 进 / 0.48 挡）、维度不符安全退化、开关关闭时不碰语义通道、
  2000 条批量入库性能。
- 新增 `tests/test_kb_model_pick.py`（39 组断言：三条挑选路径、失效回落与提示、展示名、
  配置键贯通、前端下拉与兜底、防 dict 回归）。
- 新增 `tests/ui_kb_fold_check.py`：折叠区必须存在且**默认收起**、展开后仍能读到「实测 / 代价 / 前提」，
  同排两卡内容高度差 ≤ 120px、知识库卡内容高度 ≤ 620px（长文案一塞回去就报警）；
  另钉三条排版契约：**任何开关行都不许跨列**（宽度不得超过网格半栏的 1.2 倍）、
  **同一行的两格必须等高**（两格虚线不许一高一低）、「语义增强检索」副标题里必须保留
  「仅推荐大型长期运行服务器开启」这句提醒。
- 新增 `tests/ui_broadcast_order_check.py`：无头浏览器实测「控制台 tab 名称」与
  「文本颜色三格**按视觉位置排序后**的中文标题」必须完全一致，设置页三段标签同序同名；
  并逐格核对输入框归属（标着「聊天栏」的那格必须装着 `clr_say`，改顺序时不许串台），
  外加断言配色说明写的是「下方」而不是「上方」。
- 回归全绿：`test_kb_search_engine`、`test_kb_rerank`、`test_kb_model_pick`、
  `test_help_and_reload`、`ui_card_layout_check`（12 个网格容器 0 问题）、
  `ui_settings_structure_check`、`ui_sw_wrap_check`、`ui_theme_check`、`ui_kb_fold_check`、
  `ui_broadcast_order_check`。

## [v0.21.40] - 2026-09-15（预发布）

> 一句话：知识库检索换了新引擎（BM25），查得准多了 —— 旧版保留成可选项。
>
> 本版为**预发布（pre-release）**：先把检索引擎换代这一步落地，后续还有别的改动要做，
> 等那一批做完再发正式版。想要稳定版请用 v0.21.39。

### 变更
- **知识库检索引擎换代，BM25 成为默认**（`core/knowledge_base.py`）：
  中文改为**二元切分（bigram）**，按字序匹配 —— 查「黄铜」不再捞出「铜黄」，查 `tac` 也不再
  捞出 `taconite`；配合 BM25 的 idf 权重自动压低「通用 / 方案 / 规则」这类高频套话，
  长条目不会靠字数多而霸榜，再由最低分门槛砍掉尾部噪声。
  本机实测（26 条语料 / 17 组查询）：结果精确率 **20.5% → 58.6%（2.9 倍）**，
  无关条目 **66 → 12（减少 82%）**；5000 条规模单次检索 **6.3ms → 2.0ms**
  （代价：写操作后重建索引约 0.03ms/条，5000 条约 144ms，仅写后一次）。
- 新增配置项 **`knowledge_search_engine`**（设置页「词典与知识库」卡片）：
  `bm25`（默认）/ `legacy`（旧版检索，字面与历史结果完全一致）。线上默认自动切到 BM25，
  想要旧行为把这一项改成 `legacy` 即可；保存设置即时热切换，**无需重载插件**。
  非法值一律回落默认，检索不会因配置写错而报错。
- 倒排索引生命周期挂在 `load()` / `save()` 上：沉淀、纠错、启停、删除条目后**立即**跟上。

### 修复
- `set_search_engine()` 在切到 `legacy` 时未释放索引对象（切回 BM25 前的短暂窗口里
  `_index` 仍是旧对象）。现在两个方向都走一次重建：BM25 建索引、legacy 释放索引。
- `ModKnowledgeBase.save()` 落盘后未触发索引重建（只在 `load()` 里建），
  导致「刚沉淀的条目在自己这次进程里检索不到」。现在 `save()` 之后同样重建。

### 工程
- 新增 `tests/test_kb_search_engine.py`（33 组断言）：默认值 / 非法值回落、两引擎排序一致性、
  误召回对比、字序敏感、词边界、索引生命周期、待审条目隔离、模板补召、热切换、空查询边界。
- 验证数据全部由脚本实测产出，写入 `main.py` 版本注释、`_conf_schema.json` 与设置页文案
  （不写估算值）。

## [v0.21.39] - 2026-09-15

> 一句话：权限文案全链路说清楚了、修了两个文件头的 BOM、把发版流程自动化了。

### 变更
- 权限策略口径统一：白名单下「非管理员完全不能使用**口头命令工具**（执行指令 / 发物品 / 广播），插件自带功能照旧」。
  覆盖 README、WebUI 设置页策略卡片、`mcs 帮助`、配置说明（`docs/configure.md` 与 `_conf_schema.json`）与市场文案（`metadata.yaml`）。

### 修复
- `core/agent_llm.py`、`core/workflow.py` 文件头的 UTF-8 BOM（会让 `ast.parse`、linter 等静态工具直接报
  `invalid non-printable character U+FEFF`）。
- `requirements.txt` 注释里的反斜杠笔误（`\mcs` → `mcs`）。

### 工程
- 新增 GitHub Actions：`tests.yml` 在 push / PR 时自动跑回归测试；`release.yml` 推送 `v*` 标签即自动打包并发布 Release。
- 新增 `.editorconfig`（UTF-8 无 BOM + LF），防止 BOM / CRLF 再次混入。
- 新增 Issue 模板（Bug 反馈 / 功能建议）与 README 状态徽章。
- Release 包不再携带 `tests/`（`.gitattributes` 的 `export-ignore`），包体更小。

## [v0.21.38] - 2026-09-13

### 新增
- 上架 AstrBot 插件市场（审核通过，VirusTotal 零检出）。
- 文档新增四个页面：界面效果、配置详解、使用指南、常见问题；安装段补充「应用市场安装（推荐）」与 Releases 入口。

### 变更
- 元数据补齐 `short_desc` / `astrbot_version` / `support_platforms`；权限策略表述不再依赖行内代码渲染。

## [v0.21.37] - 2026-09-13

### 新增
- 首个公开版本：RCON 远程指令与 LLM 工具、`mcs` 指令组（绑定 / 喊话 / 状态 / 踢人封禁）、
  玩家事件播报、物品词典与整合包指纹、多 Agent 工作流与知识库、十组可视化设置与六页插件 WebUI（含深浅主题）。
