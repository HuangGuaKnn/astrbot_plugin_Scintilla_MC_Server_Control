# 更新日志

本项目的所有重要变更都记录在这里。格式参考
[Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循语义化版本。

> **写作约定**：每个版本小节的第一行请用一行引用块写「本版一句话」摘要
> （`> 一句话：…`）。发布 Release 时会自动把小节正文当作说明，
> 这句摘要就会出现在最外层，访客不展开细节也能看懂这一版干了什么。

## [v0.22.0] - 2026-09-18

> 一句话：知识库检索一口气连升三档 —— 「换种说法也能查到」的语义通道、召回之后的精排、以及手动指定嵌入 / 重排模型；顺带把设置页那一排卡片的高度、广播页的叫法都收拾齐了。

> 本节汇总了 v0.21.41 ~ v0.21.45 五个未单独发布的预发布改动，明细一条没删。

### 新增

#### 语义增强检索（原 v0.21.41）
- **语义增强检索**（配置项 `knowledge_semantic_search`，设置页「词典与知识库」卡片，
  **默认关闭**）：知识库在 BM25 词法通道之外，再走一条「嵌入向量」语义通道，两条通道
  各排各的名次、再用 **RRF（倒数排名融合）** 合并。开启后提问可以「换种说法」——
  问「后台连不上了」也能捞到库里那条「RCON 连接失败排查」。
  - **只建议大型服务器启用**：每次检索多一次嵌入调用（约 300~500ms 延迟）；建库时每条
    约一次嵌入调用（后台分批补算、只算新增 / 改动过的条目、结果落盘缓存不重复烧）。
    条目少的小库用 BM25 已经够准，这份开销不值。
  - **前提**：需先在 AstrBot「服务提供商」添加一个 Embedding 模型（推荐文本嵌入模型）。
    没配置时开关开了也不报错，安静退化为纯 BM25。
  - 本机实测（438 条语料 / 24 组带标准答案查询 / 槽位 K=6）：

    | 检索方式 | Hit@1 | Hit@6 | MRR | 无关查询误召回 |
    | --- | --- | --- | --- | --- |
    | 纯 BM25 | 75.0% | 79.2% | 0.771 | 0 |
    | ＋语义（无门槛） | 83.3% | 91.7% | 0.868 | 18 |
    | **＋语义（余弦门槛 0.50）** | **87.5%** | **100.0%** | **0.931** | **0** |

    拆分看：字面型查询 Hit@1 91.7% → 100%；口语改写型查询 Hit@1 58.3% → 75.0%、
    Hit@6 58.3% → 100%。换多模态嵌入模型时语义通道被压弱但**仍不低于纯 BM25**，不会把命中挤掉。
  - 新增余弦相似度门槛 `SEMANTIC_MIN_SIM = 0.50`：RRF 只看名次、不看分数，无关提问照样会被
    塞进候选池并加分（实测 3 条无关查询的返回条数从 0 涨到 18）。加一道绝对门槛后误召回归零、
    指标反而更高。相关查询 top1 余弦最低 0.505、无关查询最高 0.460，0.50 正好卡在两者之间。

#### 重排序精排（原 v0.21.42）
- **重排序精排**（配置项 `knowledge_rerank`，设置页「词典与知识库」卡片，**默认关闭**）：
  检索升级为「召回 → 精排」两段式 —— 先用 BM25（可叠加语义通道）召回 `RERANK_POOL = 20`
  条候选池，再交给 **Rerank 模型（Cross-Encoder）** 逐条打分重排，最后取前 `limit` 条。
  召回看的是「词 / 向量像不像」，精排看的是「查询与条目的真实相关性」，能把召回到了但
  排不前的正确条目提上来。
  - **与语义通道相互独立**：没有 Embedding 模型也能单独开（只对 BM25 召回池精排）；
    两者同开就是「BM25 + 向量 RRF 召回 → rerank 精排」的完整三段式。
  - **前提**：需先在 AstrBot「服务提供商」添加一个 Rerank 模型（如 `vllm_rerank` /
    `Qwen3-Reranker-8B`）。取已加载的第一个 Rerank Provider，不按 id 硬编码。
    没配置时开关开了也不报错，安静退化为召回顺序。
  - **代价**：每次检索多一次重排 API 往返。小库（几十条）BM25 已经够准，不值得开。
  - 失败即退化：rerank 调用异常时自动退回普通检索，精排永远不该让整次检索失败。

#### 手动指定嵌入 / 重排序模型（原 v0.21.43）
- **手动指定嵌入 / 重排序模型**（配置项 `knowledge_embed_provider_id`、
  `knowledge_rerank_provider_id`，设置页「词典与知识库」卡片，**默认留空 = 自动**）：
  - 留空 = 用 AstrBot 里加载的第一个（与之前的行为完全一致，老配置无需改动）。
  - 填了就用指定的那个 —— 两台嵌入模型（比如一个本地小模型、一个远程大模型）时可以按需挑。
  - 支持空间有限：WebUI 设置页两个下拉直接从 AstrBot **已加载的 Provider 实例**读取，
    「配置里有但没启用 / 加载失败」的模型不会出现在候选里 —— **下拉能选的一定可用**，
    不用手抄 provider ID。
  - 指定失效（模型被删除 / 停用 / 改名）时**安静回落到第一个**，保存设置时提示
    「已回落」并写明实际生效的是哪一个，不会让检索失效、也不会让人误以为指定的生效了。
  - 保存即生效（无需重载插件）：换 **嵌入模型** 会触发**后台整库重算向量**
    —— 不同模型的向量维度与语义空间都不通用，留着旧的会串味；换 **Rerank 模型**
    只换注入，无需重算。
  - 「当前生效」会在设置页直接标出来（`自动` 还是 `指定值 · 模型名`）。

### 修复

- **知识库当前生效模型永远显示为空**：`kb_model_status` 把「给前端下拉的 dict 列表」喂给了
  挑模型函数，而 dict 上没有 `provider_config` —— 导致当前生效模型显示为空、失效回落永不触发。
  改为统一走 Provider **实例列表**（`_provider_insts`），并由测试钉住（新增回归护栏）。（原 v0.21.43）
- **向量入库 O(n²) 复制**：`SemanticIndex.set_vectors()` 原本每加一行就 `vstack` 一次，
  5000 条要 21 秒。改为整批拼接后 0.14 秒（**提速约 150 倍**），批量建库不再卡顿。（原 v0.21.41）
- **换嵌入模型时拿旧向量硬算**：向量维度与缓存不符时明确放弃本次语义通道、退化为纯 BM25。
  （原 v0.21.41）
- **广播控制台的配色说明写反了方位**：原文写「颜色跟随**上方**「文本颜色」设置」，
  可颜色卡其实在它**下面**，已改为「下方」，并直接点明「各用同名那一格的颜色」。（原 v0.21.45）

### 变更

- **设置页排版：知识库卡的说明收进折叠区**（`details.kb-fold`，**默认收起**，点标题展开 / 收起）：（原 v0.21.44）
  - 原先这张卡靠 5 段长 hint 堆到约 1260px，同排的「LLM 工具开关」被 grid 拉伸后，
    底部空出 **814px** —— 看起来像卡片没写完。现在两卡内容高度 463px / 496px，空档 **50px**。
  - 7 个开关收成两列（词典 2 + 知识库工具 3 + 语义增强检索 + 重排序精排）；两个模型下拉并排成一行。
  - **「重排序精排」不再横跨两列**（曾写成 `grid-column:1/-1`，一个开关占两格、看着像没写完）——
    恢复单格，与左列开关同一竖直位置。
  - 「语义增强检索」副标题改为「加嵌入向量通道（仅推荐大型长期运行服务器开启）」：
    文案长了一档，在括号前主动断行，避免折成「……长期运行服务\n器开启」这种半截句；
    两行副标题会让同行两格不等高（虚线一高一低），因此 `.kb-card .sw-grid` 改用
    `align-items:stretch` —— 短的那格虚线照样落在行底，仍是一条直线。
  - 原理解释、实测数据、代价与前提**一字未删**，只是从卡片正文挪进折叠区
    （检索引擎 / 语义增强检索 / 重排序精排 / 模型回落 / 生效时机，共 4 段）。
  - 折叠区样式只作用于 `.kb-card`（`details.kb-fold`），全局 `details` / `summary` 规则未动。
- **广播控制台与「文本颜色」的叫法、顺序统一**（原 v0.21.45）：广播控制台的三个模式是
  「聊天栏 → 全屏标题 → 模拟任务输出」，下面「文本颜色」卡片却是「全屏喊话 → 普通喊话 → 任务输出」
  ——顺序反着、叫法也不一样。现在三格改成「聊天栏 / 全屏标题 / 模拟任务输出」，与上方 tab 一一对应；
  **设置页**那张「文本颜色」卡里的三段「单色 / 渐变锚点」也一并调成同一顺序、同一叫法。
  原生配置页与文档跟着统一叫法（`color_say` / `color_title` / `color_feedback` 三组颜色与渐变锚点），
  只改显示名，配置键与默认值**一个没动**（老配置照旧生效）。
  注意 `mcs 全屏喊话` 是**指令名**，与「全屏标题」这个模式名不是一回事，故保持原样未改。

### 工程

- 取模型路径收敛成三处共用：`_resolve_embed_fn` / `_resolve_rerank_fn` / `kb_model_status`
  都走 `_provider_insts()` + `_pick_provider()`，不再各自 `insts[0]` 硬取第一个。（原 v0.21.43）
- 召回逻辑从 `search()` 抽成 `_rank_candidates()`、模板补召抽成 `_template_fill()`，
  普通检索与精排检索**共用同一套召回与补召口径**，避免两条路径行为漂移。（原 v0.21.42）
- `asearch()` 分流：精排开启且注入了可用调用时走 `_search_with_rerank()`，否则维持原路径。
  精排开关热生效：保存设置即注入 / 撤销 rerank 调用，无需重载插件；切换预设、
  换服务端重建 KB 实例时同步带上开关状态与 rerank 调用。（原 v0.21.42）
- 设置页与配置页文案里的实测数值全部对齐到本次复测结果（不写估算值）。

### 测试

- 新增 `tests/test_kb_semantic_search.py`（14 组断言，纯离线桩向量、不烧 API）：
  融合确实生效、门槛上下边界（0.52 进 / 0.48 挡）、维度不符安全退化、开关关闭时不碰语义通道、
  2000 条批量入库性能。
- 新增 `tests/test_kb_model_pick.py`（39 组断言：三条挑选路径、失效回落与提示、展示名、
  配置键贯通、前端下拉与兜底、防 dict 回归）。
- 新增 `tests/ui_kb_fold_check.py`：折叠区必须存在且**默认收起**、展开后仍能读到「实测 / 代价 / 前提」，
  同排两卡内容高度差 ≤ 120px、知识库卡内容高度 ≤ 620px（长文案一塞回去就报警）；
  另钉三条排版契约：**任何开关行都不许跨列**（宽度不得超过网格半栏的 1.2 倍）、
  **同一行的两格必须等高**（两格虚线不许一高一低）、「语义增强检索」副标题里必须保留
  「仅推荐大型长期运行服务器开启」这句提醒。
- 新增 `tests/ui_broadcast_order_check.py`：无头浏览器实测「控制台 tab 名称」与
  「文本颜色三格**按视觉位置排序后**的中文标题」必须完全一致，设置页三段标签同序同名；
  并逐格核对输入框归属（标着「聊天栏」的那格必须装着 `clr_say`，改顺序时不许串台），
  外加断言配色说明写的是「下方」而不是「上方」。
- 回归全绿：`test_kb_search_engine`、`test_kb_rerank`、`test_kb_model_pick`、
  `test_help_and_reload`、`ui_card_layout_check`（12 个网格容器 0 问题）、
  `ui_settings_structure_check`、`ui_sw_wrap_check`、`ui_theme_check`、`ui_kb_fold_check`、
  `ui_broadcast_order_check`。

## [v0.21.40] - 2026-09-15（预发布）

> 一句话：知识库检索换了新引擎（BM25），查得准多了 —— 旧版保留成可选项。
>
> 本版为**预发布（pre-release）**：先把检索引擎换代这一步落地，后续还有别的改动要做，
> 等那一批做完再发正式版。想要稳定版请用 v0.21.39。

### 变更
- **知识库检索引擎换代，BM25 成为默认**（`core/knowledge_base.py`）：
  中文改为**二元切分（bigram）**，按字序匹配 —— 查「黄铜」不再捞出「铜黄」，查 `tac` 也不再
  捞出 `taconite`；配合 BM25 的 idf 权重自动压低「通用 / 方案 / 规则」这类高频套话，
  长条目不会靠字数多而霸榜，再由最低分门槛砍掉尾部噪声。
  本机实测（26 条语料 / 17 组查询）：结果精确率 **20.5% → 58.6%（2.9 倍）**，
  无关条目 **66 → 12（减少 82%）**；5000 条规模单次检索 **6.3ms → 2.0ms**
  （代价：写操作后重建索引约 0.03ms/条，5000 条约 144ms，仅写后一次）。
- 新增配置项 **`knowledge_search_engine`**（设置页「词典与知识库」卡片）：
  `bm25`（默认）/ `legacy`（旧版检索，字面与历史结果完全一致）。线上默认自动切到 BM25，
  想要旧行为把这一项改成 `legacy` 即可；保存设置即时热切换，**无需重载插件**。
  非法值一律回落默认，检索不会因配置写错而报错。
- 倒排索引生命周期挂在 `load()` / `save()` 上：沉淀、纠错、启停、删除条目后**立即**跟上。

### 修复
- `set_search_engine()` 在切到 `legacy` 时未释放索引对象（切回 BM25 前的短暂窗口里
  `_index` 仍是旧对象）。现在两个方向都走一次重建：BM25 建索引、legacy 释放索引。
- `ModKnowledgeBase.save()` 落盘后未触发索引重建（只在 `load()` 里建），
  导致「刚沉淀的条目在自己这次进程里检索不到」。现在 `save()` 之后同样重建。

### 工程
- 新增 `tests/test_kb_search_engine.py`（33 组断言）：默认值 / 非法值回落、两引擎排序一致性、
  误召回对比、字序敏感、词边界、索引生命周期、待审条目隔离、模板补召、热切换、空查询边界。
- 验证数据全部由脚本实测产出，写入 `main.py` 版本注释、`_conf_schema.json` 与设置页文案
  （不写估算值）。

## [v0.21.39] - 2026-09-15

> 一句话：权限文案全链路说清楚了、修了两个文件头的 BOM、把发版流程自动化了。

### 变更
- 权限策略口径统一：白名单下「非管理员完全不能使用**口头命令工具**（执行指令 / 发物品 / 广播），插件自带功能照旧」。
  覆盖 README、WebUI 设置页策略卡片、`mcs 帮助`、配置说明（`docs/configure.md` 与 `_conf_schema.json`）与市场文案（`metadata.yaml`）。

### 修复
- `core/agent_llm.py`、`core/workflow.py` 文件头的 UTF-8 BOM（会让 `ast.parse`、linter 等静态工具直接报
  `invalid non-printable character U+FEFF`）。
- `requirements.txt` 注释里的反斜杠笔误（`\mcs` → `mcs`）。

### 工程
- 新增 GitHub Actions：`tests.yml` 在 push / PR 时自动跑回归测试；`release.yml` 推送 `v*` 标签即自动打包并发布 Release。
- 新增 `.editorconfig`（UTF-8 无 BOM + LF），防止 BOM / CRLF 再次混入。
- 新增 Issue 模板（Bug 反馈 / 功能建议）与 README 状态徽章。
- Release 包不再携带 `tests/`（`.gitattributes` 的 `export-ignore`），包体更小。

## [v0.21.38] - 2026-09-13

### 新增
- 上架 AstrBot 插件市场（审核通过，VirusTotal 零检出）。
- 文档新增四个页面：界面效果、配置详解、使用指南、常见问题；安装段补充「应用市场安装（推荐）」与 Releases 入口。

### 变更
- 元数据补齐 `short_desc` / `astrbot_version` / `support_platforms`；权限策略表述不再依赖行内代码渲染。

## [v0.21.37] - 2026-09-13

### 新增
- 首个公开版本：RCON 远程指令与 LLM 工具、`mcs` 指令组（绑定 / 喊话 / 状态 / 踢人封禁）、
  玩家事件播报、物品词典与整合包指纹、多 Agent 工作流与知识库、十组可视化设置与六页插件 WebUI（含深浅主题）。
